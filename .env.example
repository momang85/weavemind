# 织光 (ZhiGuang) 环境变量模板
# 注意：真实配置统一放在 config.json（已加入 .gitignore），.env 仅作参考模板
LLM_API_KEY=YOUR_API_KEY
LLM_BASE_URL=https://api.siliconflow.cn/v1
LLM_MODEL=deepseek-ai/DeepSeek-V3
REDIS_HOST=localhost
REDIS_PORT=6379
# 行情排行缓存 TTL（秒）：Redis 优先，不可用时进程内内存兜底
QUOTE_CACHE_TTL=600
