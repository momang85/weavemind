# 密钥与本地配置
.env
.env.local
config.json
config.local.json

# 数据库、记忆与指标
*.db
*.db-shm
*.db-wal
chroma_memory/
chroma_memory_test/
chroma_memory_verify/
metrics.csv
metrics_summary.json
evolution_history.json
consolidation_stats.json

# 日志
logs/
*.log

# Python
__pycache__/
*.py[cod]
.venv*/
venv/

# 本地训练产物（模型权重/LoRA）
models/
lora_out/
distill_v2_raw.jsonl
distill_data_v2.jsonl
distill_test_v2.jsonl
distill_data_merged.jsonl

# 前端
# 前端：node_modules 与构建中间产物忽略；dist 入库（新用户免 Node 直接可用，
# 前端源码改动后需 `rm -rf frontend/dist && npm run build` 干净重建再提交）
frontend/node_modules/
frontend/tsconfig.tsbuildinfo

# 运行状态
.weavemind/
# 旧运行时目录（启动时自动迁移，保留忽略规则防漏网）
.weavimind/
poison_strategies.json
prompts/overrides.json
skills/lessons.jsonl
evals/calibration_report.json

# 编辑器 / 系统
.vscode/
.idea/
Thumbs.db

# 运行时产物
share_links.json
logs/audit.jsonl

# Mimosa 扫描工件（运行期事件台账，不入库）
.mimosa/

# 本地运行产物（防误提交）
logs/
agents.db
workspace/
.venv-train/
.venv/
metrics_summary.json

# 本地配置备份（含真实凭据，绝不入库）
config.json.bak
.zcode/

# 运行时不需要的内容（已从跟踪移除，永久不上传）
标准/
distill_data.jsonl
_p13_cache_check.py
.tmp/
