# 织光项目历史与执行协作画像

日期：2026-09-14。用途：为架构决策和 DeepSeek 执行监督提供历史背景。

## 证据范围

定向读取 Zcode 当前织光主任务（`sess_0d05eff0-184c-452a-abfa-67927490b96e`）的本地历史库，使用 SQLite `mode=ro`，未修改 Zcode 数据。筛选 09-02 至 09-14 的用户输入记录，抽查关键回复和工具结果，对照 Git 提交及项目路线文档；没有逐条复核全部调用，也没有重跑历史实验。

应用标记为用户输入的记录包括“继续”、重复发送、粘贴报告和转交指令，不能等同于独立需求数。该任务先后使用 GLM-5.3/Flash、DeepSeek Flash/Pro 等型号；以下描述的是这条工作流中观察到的执行模式，不是对单个模型能力的普遍评价。可读历史从 09-02 开始，不能据此断言覆盖项目最初搭建的全部过程。

## 用户期望如何演进

| 时间及记录 | 明确期望 | 当前如何继承 |
|---|---|---|
| 09-04 11:41、13:19 | 重要修复前保留版本；审查架构和安全；真实复杂任务、前后端与视觉检查 | 修复有可恢复基线，代码测试之外还需相关用户流程验证 |
| 09-04 22:31；09-05 多次试运行要求 | 报告不仅生成，还要渲染正确、有用；解释 23 分钟耗时并优化；测试不同任务 | 以交付质量和耗时为验收项，不能只看服务启动与一条成功样例 |
| 09-05 20:43–21:08；09-06 17:02–17:20 | 按市场与用户需求规划，并转为可执行任务 | 日报、溯源 API、私有化曾进入路线讨论；具体定价与市场数字仍属历史方案，需另行验证 |
| 09-06 19:06；09-10 15:42 | 公共仓库不得泄露密钥与个人配置；清理 URL 中的凭据 | 发布与诊断都要避免输出秘密；历史授权只解释当时操作，不自动扩为新授权 |
| 09-11 18:38 | 主备供应商治理、验收器对抗测试、规则版本、成本归因、计划一致性、可回放审计事件 | 这些是用户明确提出的长期架构需求，不能只因文件或界面已存在就勾选完成 |
| 09-11 22:43；09-12 及 09-13 | 减少 Docker 依赖；一键启动、自动检测安装依赖、中文交互配置；国内新环境可用 | 保留易部署和低门槛；缺隔离时停用代码执行，不能直接否定整个工作台可启动性 |
| 09-12 16:05 | 状态单一写入责任、内存降为缓存、完整模型设置面板 | 核查状态迁移和配置持久化的真实调用链，不能把“同一模块”误认为“单一进程写者” |
| 09-14 当前用户决策 | 最终服务银行和大型机构；Codex 决策审查，DeepSeek 实现 | 调整安全默认值、证据与权限边界；旧 C 端获客和小团队方案不再决定主路线 |

历史商业文档存在“有本地模型即可保证不出域”“数字有出处即可视为可信”等超出验证范围的表述，市场规模、价格与转化目标也不应直接作为当前事实。保留原文作为历史，新的架构决策另行注明适用范围和证据。

## 当前状态：按证据强度区分

- **历史调用结果支持的交付**：09-13 22:37 的工具输出显示 18 项相关测试通过；新环境日志显示交互配置、自动安装 13 个缺失包、端点复查及 16/16 服务启动。证明该次环境流程跑通，不证明所有环境或全部金融业务都可用。对应后续安装修复可在 `c8462b7`、`309a4b6`、`5a058e1` 等提交追踪。
- **已提交、待独立复核**：`da4a7c5` 补 CI 与部署清单；`8631466` 修任务落库/收执；`acf8542` 统一可变数据根并增加容器 CI。提交说明和 DeepSeek 自报“全绿”不能替代本批实际验证。
- **当前进行中**：代码沙箱修复。17:13 明确回复收到架构指令；17:14 承认旧方案保留 auto 降级和 FALLBACK，与新决策冲突；17:16 后开始按新要求修改。已看到同步修改状态消费点及文档，尚未验收最终测试。
- **仍需跟踪**：根任务预算与步骤归因、金融事实的主体/指标/期间/单位绑定，以及机构权限和证据流。原审查中的问题是否解决，以当前差异和回归证据逐项判断。

## 如何有效使用这位执行者

| 观察 | 证据与含义 | 监督方式 |
|---|---|---|
| 熟悉仓库，能跨层排错 | 多次沿启动器、依赖、配置、UI、Redis 调用链定位问题；能用新克隆和空环境复现 | 交给它边界清楚的纵向修复，提供失败条件和验收标准，避免重复让它全仓调研 |
| 会响应具体纠正 | 本次明确承认沙箱策略冲突，再梳理状态接口、启动器与文档影响面 | 用“错误行为 → 目标行为 → 必须证明什么”下指令，比笼统说“再优化”有效 |
| 强调兼容与继续运行 | 与用户长期要求无 Docker、新手启动一致；也使宿主回退被保留下来 | 区分服务可用性和安全边界：允许报告功能不足，不允许为成功率放宽隔离 |
| 实现多、完成口径有时过宽 | 历史总结将本地运行、策略记忆、来源检查提升为完整安全/可信能力；原审查发现跨模块缺口 | 分开核验“已实现、已测试、已实跑、适用边界”，重点审接口贯通和失败路径 |
| 网络与工具障碍会消耗较多回合 | 历史有多轮代理/直连推送重试、长时间等待；用户多次追问进度 | 同路径两次失败后报告具体阻塞，切换独立工作；不做高频空轮询 |
| 输出裁剪可能弱化验证证据 | 历史命令常把测试或推送输出接到 tail/grep；某些结果确实通过，但管道成功本身不足以验收 | 保留真实退出码；CI 必须绑定提交 SHA；不能把 tool completed 当测试通过 |

09-04 的记录包含用户明确批准暂时停用特定插件门禁，因此不能仅看后续“挪开钩子”就判定全部越权。当前监督应要求明确记录适用范围与恢复状态，不能将历史例外扩展成绕过安全边界的常规做法。

## 管理决策

保留已有的一键启动、真实任务验证、报告可视化和领域适配能力；停止用历史商业叙述判断生产成熟度。后续每批只要求简短的缺陷、变更、测试、未验证项和阻塞记录。Codex 重点审跨模块契约与架构边界，DeepSeek 持续承担编码和定向测试。

为节省重复读取成本，后续先读 `DeepSeek执行状态.md` 与 `架构监督记录.md`。确需追溯时，在本地库 `C:/Users/ding0/.zcode/cli/db/db.sqlite` 内只读上述主任务的 `message`、`part`，按角色、时间和类型定向查询；不读取密钥配置，不导出完整日志或推理内容。
