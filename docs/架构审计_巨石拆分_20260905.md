# 架构缺点审计与巨石拆分进度（2026-09-05 第二轮）

上一轮架构评审指出 8 项缺点。本轮（P0 落地 + C2 深化拆分）后的逐项核验矩阵与下一步拆解计划。

---

## 一、缺点解决状态矩阵

| # | 缺点 | 状态 | 证据/说明 |
|---|------|------|----------|
| 1 | 巨石模块（orchestrator 6600 行 / web_ui 3400 行） | **部分解决** | C2 完成：图表域三方法（~156 行）迁入 `charts_pipeline/` 混入模块（192 行），orchestrator 6594→6439；全量回归零回归。**剩余**：结构化数据域 ~719 行、端到端验证域 ~357 行、模板固化域 ~258 行、`run` 主循环 866 行仍在编排器内（见第二节拆解计划） |
| 2 | 状态散落三层（webui 内存/SQLite/Redis） | **部分解决** | 会话已 SQLite 持久化（C1）。**剩余**：运行中任务状态（_task_results 内存态）与任务历史（task_history）仍双轨，webui 重启后运行中任务内存态丢失（steps_json 只补了完成态） |
| 3 | Redis 硬单点（派发强依赖） | **未解决** | `_publish_task` 仍要求 Redis 存活才可派发，无本地降级队列。已列入后续 P1/P2 评估，优先级低于功能线 |
| 4 | 反思闭环无预算约束 | ✅ **已解决** | B1：按链路分级预算（排行 2/金融 3/通用 3，config 热重载），耗尽如实收口 SUCCESS_WITH_ISSUES |
| 5 | 运行时会写源码树 | **部分解决** | templates.json 仍被 `_consolidate_template` 运行时写回仓库目录（L758/L1027）；eval 自动生长仍写 evals/cases/。**未改**——固化/生长机制是产品特性，改为可配置输出目录即可（低优先） |
| 6 | 前端双通道同步（2s 轮询 + SSE 并行） | **未解决** | useTaskPoller 与 SSE 双活，各自去重。收敛为单推送源需前端重构，列入 P1 尾部 |
| 7 | TaskConsole 单页臃肿 | **未解决** | 提交/计划/日志/报告/确认集中一个组件。与 #6 一并列入前端重构项 |
| 8 | 会话即内存 | ✅ **已解决** | C1：sessions 表持久化，重启自动恢复（单测 + 线上重启验证通过），过期条目随访问清理 |

**本轮净进展**：8 项中 2 项彻底解决（#4/#8）、2 项实质性推进（#1 C2、#2 C1），4 项待办已量化并排入后续。

## 二、巨石拆分计划（已量化）

| 候选域 | 规模 | 拆分方式 | 风险 |
|--------|------|---------|------|
| 结构化数据域（preload/injection/merge ×5） | ~719 行 | `structured_pipeline/` 混入类，同 C2 手法 | 低——self 依赖少，测试覆盖充分 |
| 端到端验证域（e2e + playwright） | ~357 行 | `e2e_verify.py` 独立模块，orchestrator 薄委托 | 低——功能自包含 |
| 模板固化域（consolidate + route） | ~258 行 | `templates_pipeline.py`；顺带解决 #5（输出目录可配置） | 中——涉及 templates.json 读写 |
| `run` 主循环 | 866 行 | 最后一步：按阶段拆为 execute/report/confirm/reflect 四个方法 | 高——核心控制流，需行为级回归兜底 |

目标：orchestrator_v2 ≤ 4500 行后，`run` 再瘦身至 ≤ 500 行（只保留控制流骨架）。

## 三、web_ui（3443 行）拆解建议

- 会话/认证块（~150 行，C1 已集中）→ `authz.py`
- share 页与结构化解析（~250 行）→ `share_pages.py`
- 报告数据组装（_get_task_report_data/_share_page_structured/~300 行）→ `report_views.py`
- 路由表与 handler 分派保留在 web_ui，各 handler 薄委托
- 目标：web_ui ≤ 2200 行，路由层纯薄

## 四、本轮 C2 落地明细

- 新增 `charts_pipeline/__init__.py`（192 行）：ChartPipelineMixin 承载 `_render_clean_chart_data` / `_render_chart_data` / `_generate_search_charts` 三方法原样搬迁（含全部注释与容错分支）
- `class OrchestratorV2(ChartPipelineMixin)` 继承混入；方法签名/行为零变化，测试零改动
- 两个修复点：repo_root 上溯两级（__file__ 指向包目录）；`_sanitized_process_env` 延迟导入避免循环依赖
- 回归：test_common ✅ / orchestrator 65 ✅ / delivery 176 ✅ / p0 266+ ✅ / CI 评测闸门 ✅
