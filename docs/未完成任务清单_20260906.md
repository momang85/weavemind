# 未完成任务清单与执行记录（2026-09-06）

来源：《全项目代码审查报告》四、建议的下一步 + 架构审计计划剩余项。
优先级：T1-T4 为后端能力缺 UI（高价值）；T5-T8 为对齐与运维；T9-T11 架构重构（视时间）。

| # | 任务 | 状态 |
|---|------|------|
| T1 | 设置页：通知配置 UI | ✅ 完成（三通道开关+字段+保存，浏览器核验） |
| T2 | 设置页：定时任务管理 UI | ✅ 完成（列表/启停/删除/新增） |
| T3 | 审计日志页面 /audit | ✅ 完成（200 条留痕渲染） |
| T4 | 指标看板页面 /metrics | ✅ 完成（成功率/耗时/成本/能力分项） |
| T5 | 项目选择器接 /api/projects | ✅ 完成（datalist 候选） |
| T6 | 单智能体直发入口 | ✅ 完成（Agents 页直发面板） |
| T7 | metrics 延迟链路对齐 | ✅ 完成（elapsed_sec 进延迟统计，单测验证） |
| T8 | 沙箱镜像构建 | ✅ 完成（355MB：python:3.12-slim + node v20 + DroidSansFallbackFull 中文 TrueType；途径：Docker 引擎走宿主代理 + apt 切阿里云源） |
| T9 | 端到端验证域抽取（~357 行 mixin，同 C2 手法） | ✅ 完成（e2e_verify 模块 + 薄委托，commit 9bd07cd） |
| T9b | 清理 _run_e2e_verification/_playwright_verify 历史主体（dead code） | ✅ 完成（-400 行，orchestrator 5675→5276 行） |
| T10 | 模板固化域抽取（~258 行 + 输出目录可配置） | ✅ 完成（第一阶段：templates_pipeline 模块含 normalize_steps/topic_tokens + 薄委托；常量晚导入保持单一来源） |
| T10b | _route_template/_consolidate_template 抽取（依赖注入后） | 排队（深度依赖 messaging/LLM/验收回调，需依赖注入改造） |
| T11 | 前端双通道收敛 SSE + TaskConsole 拆分（大改） | 排队 |
