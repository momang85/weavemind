# DeepSeek 实施任务：视觉实测纠偏

依据：视觉实测报告_20260915.md；当前 HEAD ab740b5，现有 L01 网络边界工作保留。Codex 不改实现。先收好当前单元并记证据，再按以下顺序分批修复；不重做全仓、不绕门禁、不更改模型/权限，保护 templates.json。本轮真实服务在8080，勿擅自重启覆盖实测状态。

## V1：修订稿选择与来源语义（P1，先做）

1. 复现 C 的1033字符旧稿与504字符修订选择。检查 orchestrator_v2 两处 len(cand) 比较及最终输出/检查点；按硬约束、验收结果与实质改进选版本，禁止以长短单独代替质量。
2. 对 A/C 实施用户材料及派生计算溯源，格式规则、验收器、免责声明、UI共享语义。用户输入是来源，不证明内容本身真实；计算可追溯到输入及公式。禁止降低全局阈值、自动改为模型知识、伪造URL或禁用验收。
3. 样例 A 期待20%、30%、25%、-5个百分点、12%、约6.67%，来源为虚构输入及计算；C 事实不增加企业规模/产品线结论。正文长度策略应明确，修订合格后各渠道交付同一版本。
4. B计划已注入A刚产生的“不联网、虚构数据”教训，与B官方年报检索冲突。经验只提供相关建议，不得覆盖当前来源/期间/工具限制；增加不同任务目标串扰的固定样例。未验证的失败教训不能自动升级为全局硬约束。

最小验收：不用付费LLM的固定候选稿测试，覆盖更短纠错稿被接受、更长错误稿被拒绝、两种来源混合、缺输入的计算仍被拒绝、外部无证据数字仍失败、最终页面/Markdown/PDF同版本。只跑相关回归并保留退出码。

## V2：L02预算、评审退化和取消（P1）

统一覆盖规划重试、Critic、反思、Embedding、ReAct子工具的根预算；相同来源缺口重复失败应收敛。Embedding402在任务内熔断，关键词回退与记忆写入失败分别显示。个人模式评审超时显示“未完成评审/降级”，银行必需评审按策略拒绝，不能视为通过。

最小验收：用假时钟/供应商替身覆盖超时、同类失败、主备重试相乘、停止发生于LLM调用中；请求停止后不再创建新步骤或新付费调用，已有请求的收尾上界明确；终态、UI与持久化一致。不能以mock证明真实供应商可中断。

补充实证：A停止后终态卡片为FAILED但日志为用户取消，须区分CANCELLED。B的ReAct未收敛却先发SUCCESS，再自动降级；区分工具执行成功、研究目标达成和降级交付，避免错误统计。

## V3：交付PDF与结果主视图（P2）

用 task-C-export.pdf 两页及对应Markdown复现异常斜线、暗色表头、标题孤行、需求大标题重复；检查 report_pdf.py/web_ui._task_pdf_bytes。标题采用简洁任务名，数字与单位尽量不拆分，表头可读、跨页重复表头，节标题与首内容相邻。不要把整段需求当封面标题。

最小验收：现有两页样例、长表格、无表格三种离线导出；逐页渲染检查，不以文件存在/字节数证明排版通过。结果主视图优先报告、来源、限制和下载；内部目录限制不直接面向用户。

## V4：任务进度与中文表达（P2）

LiveActivity时间统一解析到本地时区，非法值未知；提交后缩小输入区、保留清晰目标和当前阶段，已完成以交付为主；职责中文名替换原始worker名，调试提示词折叠；速览不截断数值/单位；模型模式加载中不要先显示一个具体模式。

最小验收：UTC Z/带偏移/毫秒/非法时间；408px窄屏与桌面运行/待确认/完成/失败/停止状态截图；按钮可触达，进度信息与后端一致。原始日志可以保留在展开区，不复制到最终报告。

## 交付规则

每批更新 DeepSeek执行状态.md：实际修改、测试退出码、仍未验证。先证据后结论，不宣称银行可用。门禁遇阻走已支持复核路径，不能移钩子或换终端绕过。网络边界未接入的LLM/LoRA/MCP/Embedding/webhook仍属于L01未完成，不能因视觉修复从待办消失。
