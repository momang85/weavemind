# 2026-09-16 架构复核与执行纠偏

依据：Zcode当前织光任务末尾记录、ab740b5之后工作区增量、任务数据库与日志。Codex只审查决策，DeepSeek实现。本文取代“B仍在卡住”的旧状态判断，不改变门禁、模型或权限。

## 已核实的进展

- V1新增report_quality版本比较、用户材料来源通道、AST白名单公式求值、按来源生成免责声明。真实A新报告明确2025现金流数据缺失，未把2024的12%冒充2025值，且来源声明已改为用户材料。这是有效改善，但不等于V1整体通过。
- V2增加CANCELLED终态、派发前取消检查和等待切片；Critic回退从PASS改为DEGRADED；个人降级/银行拒绝有基础实现。
- 本轮独立运行test_cancel_semantics与test_review_degradation：21项通过，退出码0，测试计时0.380秒；未跑全套、未发付费请求。下列反例是套件之外的最小离线检查。
- Zcode历史显示编排测试900秒超时后，加离线桩可在331秒结束并发现首稿未采用的真缺陷；首稿缺陷已补，但最后完整套件被中止，不能称全套通过。不要为验证继续重复900秒整套。
- 网络策略、身份协议、本轮未触及的前端/PDF/dist与上次指纹相同，未重复验收。HEAD仍ab740b5，修改未提交；templates.json保护指纹不变。

## 当前运行事实与诊断纪律

数据库当前RUNNING=0。新的A ui-7e79c26dd1于00:34:31完成，224秒SUCCESS；C ui-0e0d505a92于00:41:41完成，260秒SUCCESS。以上是系统终态，不等于业务验收通过。

B ui-4cf6e482e9于00:37:19开始，01:05:36结束，1697秒，SUCCESS_WITH_ISSUES；acceptance_json为空。报告写明使用未独立核实的模型知识，却给出财务数值和图表，未满足官方来源要求。仅作为失败/有缺口样例，不引用它的财务数字为事实。旧B ui-5712f00b24已于前日晚19:34:15收尾FAILED，不再等待其停止。

Zcode00:52停止观察时的“仍卡住”现已过时。空Redis队列不能证明没有等待；Registered agent是状态续报，不能单独证明崩溃重启；worker发布结果也不证明正确消费者已收到。避免从日志静默直接断言死锁或只需加心跳。后续复现须绑定根任务/步骤/派发ID、进程PID、开始结束时间与调用阶段；发生超时才抓对应线程栈。现样例已经结束，不以诊断旧样例为由盲目重启。新增阶段观测与根预算/重试收敛一起做，不能只改善等待文案。

## 必须先修的边界

### 0. V1发现的三个实际回归（P1，优先于扩展功能）

独立只读审查与离线内存复现确认：

1. orchestrator_v2.py:3746-3748、4048-4050将同一份最新acceptance同时传给旧稿与候选稿，无法比较两稿的验收质量；4209用旧best_report，4232读最新验收，存在交付内容和元数据错配。必须将正文、来源、验收、指纹绑定成不可混用的版本记录，修订比较和最终导出引用同一个选中版本。不能只修改compare_versions内部排序。
2. acceptance_checker.py:698-710、723把clean_chart_data的JSON与user_material文本拼接，后续结构化计算解析失败。结构化数据为2024收入1000亿元、2025收入1200亿元时，“合计2200亿元，年均1100亿元，同比增长20%”原本pass、coverage=1；仅增加user_material='请分析营收同比变化'就变fail、coverage=0。分来源通道处理，禁止拼接破坏结构。
3. acceptance_checker.py:478-493公式核验只截取局部二元式、操作数按子串匹配，并缺单位/指标绑定。用户输入1000万元、1200万元时，“收入增长120%（1200/1000-1）”“收入合计2200亿元（1200+1000）”“利润2亿元（12-10）”全部被判coverage=1、computed_count=1。分别是完整表达式被截断、金额单位错一万倍、缺失操作数却命中数字子串。不得通过放宽阈值来修正。完整表达式AST安全求值还必须有操作数token边界、来源记录、量纲与期间/指标语义；未实现部分保持未核实，不授予已验证计算标记。

最小离线反例（仅审查，不调用模型）：

```python
import json
import acceptance_checker as ac
import report_quality as rq
latest = {'overall': 'pass', 'gaps': []}
print(rq.compare_versions('营业收入999亿元。', '营业收入800亿元。',
    cur_acceptance=latest, cand_acceptance=latest)[0])  # 当前False
clean = json.dumps({'market_data': [
    {'label':'2024年营收','year':2024,'value':1000,'unit':'亿元'},
    {'label':'2025年营收','year':2025,'value':1200,'unit':'亿元'}]}, ensure_ascii=False)
for sources in ({'clean_chart_data':clean},
    {'clean_chart_data':clean,'user_material':'请分析营收同比变化'}):
    r=ac.check_number_traceability('合计2200亿元，年均1100亿元，同比增长20%。',sources,domain='financial')
    print(r['pass'],r['covered_ratio'])  # 当前True 1.0 -> False 0.0
for report in ('收入增长120%（1200/1000-1）。',
               '收入合计2200亿元（1200+1000）。', '利润2亿元（12-10）。'):
    r=ac.check_number_traceability(report,{'user_material':'2024年收入1000万元，2025年收入1200万元。'},domain='financial')
    print(r['covered_ratio'],r['computed_count'])  # 当前错误地均为1.0,1
```

新增行为测试应调用真实编排选择/导出路径，而非只证明独立比较器可用；已知fail旧稿和pass新稿用各自验收，纯数字纠错且无重复标题差异也应胜出。允许保留“不确定/待核实”，不能把无法证明的输入和计算直接认证。

### 1. 评审协议与任务归属（P1）

最小离线复现：用test_review_degradation._orch构造bank，返回{}或verdict=BOGUS，调用真实_review_plan均返回原步骤且_revise_plan调用1次；没有拒绝。当前只有ERROR/DEGRADED专门处理，其余非PASS全当普通FAIL。未知/缺失/非法裁决应视为评审未完成，银行拒绝、个人显式降级，不花额外修订调用掩盖协议错误。

真实_review_is_required在WEAVEMIND_IDENTITY_MODE=bnak时返回False：它吞掉default_mode的配置错误，不能默认转个人模式。银行必需评审还要覆盖system.critic关闭/模板/恢复入口，不能只测直接_review_plan；FAIL修订后何时重新评审和拒绝执行应明确。

_review_degraded是共享orch实例字段，而主循环多线程调用orch.run；任务互相清空该字段有串扰风险。_task_review_degraded目前仅写入，未接入最终报告、持久化与终态判定，单测只断言源码字符串不能证明交付标注。改成按根任务归属的结构化review结果，复核缺口必须随最终产物输出；加入两个并发任务一PASS一降级的交错测试。

### 2. 取消是控制结果，不是超时（P1）

最小离线复现：真实_dispatch搭配内存队列替身，模拟等待期间取消、_wait_for_result返回None，结果为FAILED / Timeout after 300s。当前None同时表示取消、超时、解析失败；需区分并贯穿调用链。

取消后不再新增模型/工具/重试/评审/反思/收尾自迭代调用，已在飞调用要有明确预算与收尾上界。继续补反思、人工确认、验收、Memory、模板、后台线程入口，不以单个Redis等待提前返回证明整条链取消。观察性心跳不能替代根任务deadline。对所有早退/异常路径验证状态、步骤、费用和持久化一致。

### 3. 交付验收与经验沉淀（P1）

B的5/5步骤成功、acceptance缺失、终态有缺口，仍记录successful_strategies待补录、模板verified 1/3、Prompt refinery applied 3 overrides。这会把未满足官方来源的产物传播成成功经验。缺验收不得默认为验证通过；有缺口可以留审计记录或待复核经验，不能增加成功计数或自动成为全局硬约束。不得简单删除历史数据或关闭所有记忆功能。

官方来源要求属于任务硬约束，检索失败应交付明确缺口，不能用模型知识的财务数值和漂亮图表代替。个人材料来源、联网授权、资料真实性、外部事实核实分别表示。A/C的长度和页面/Markdown/PDF同版本仍待核实，不以SUCCESS直接验收。

## 实施顺序与最小验证

先完成版本/证据契约和上述P1边界，再推进V2根预算与调用观测；离线回归通过后，只各做一次有界的官方来源失败、运行中取消、评审不可用验证。最后处理V3 PDF/V4中文进度；L01登记服务接线与银行可信认证仍保留为未完成前置条件。

默认测试真正禁止外网和计费调用，优先按测试名定位失败，避免模块级桩把应测行为整个替换。完整套件只在定向回归通过后跑一次并留真实退出码。Embedding402是外部额度问题，代码负责任务内熔断、降级展示和受限待补录队列，不假装补额度或自行改凭据；健康缓存应按端点分别冷却，避免一个欠费备用端点把正常主端点也冻结10分钟。

更新DeepSeek执行状态顶部为短摘要：当前批次、实际通过、未通过、下一步；过期“卡死”“待重启”等状态改为带时间的历史。每批只改必要文件、记录指纹和测试退出码；门禁按支持路径留证复核，不停钩子、不拆命令绕过，不推送。
