# 全方位端到端测试报告（2026-09-21）

范围：浏览器视觉测试 + 实机洋河任务（`ui-31305a2b28`）+ 离线场景复跑 + 结构检查器复核 +
遗留项收口（第二轮，服务重启后复测）。
基线 `ebd2ac5`；本批提交 `4aee354` / `4dc1742` / `29ec71d` / `c18d584` / `d316a25` / 收口笔（见 §6）。
时区：正文所有时刻为本地时间（UTC+8）；数据库与工作区文件存 UTC，本报告已换算并逐条对照。

## 1. 结论摘要

- 浏览器端到端测试发现 **5 个真问题**（2 个页面级、1 个渲染级、1 个装配幂等级、1 个版本绑定级），
  全部修复、提交、推送；其中 4 个由离线测试钉死（`test_delivery_chain` +6、`test_frontend_guards` +5）。
- 实机任务 11:49:48 提交、12:05:47 完成，**15 分 59 秒**（编排器自报 959s），在 25 分钟上限内；
  修订重验后状态 SUCCESS/verified，验收 pass、0 缺口。
- 修复后新任务：6 张图全部 1038×644（旧任务是 1038×121366 的异常画布）、PDF 8 页 6/6 图在页内、
  表格列对齐、离线三场景 risks 无伪影无重复。
- 第一轮遗留的四项在第二轮**全部收口**（§6）：装配候选未采纳的机制查清并按"装配候选留档"如实
  标注；评审降级确认为设计并补上页面一行（含结论所属版本）；新闻稿误标"财务附注"按来源修好并
  在实机文档上复验；Mimosa 完整扫描已跑完（46 条静态发现，2 条凭据发现核实为误报，覆盖率
  partial/inconclusive，**不宣称项目安全**）。
- 仍存疑三项（§6.5）：反思重做稿取舍（编排层策略）、214 字分析的双成因（节名过滤 + 输出上限
  耗尽）、调用台账只覆盖编排器侧（预算上限无法事后逐条核对）。

## 2. 测试范围与方法

| 层 | 方法 | 覆盖 |
|---|---|---|
| 前端渲染 | in-app 浏览器（control-browser 技能）：DOM 快照 + 计算属性读数 + 截图 | 面板区块顺序、机器/人工两行、绑定警告、包陈旧提示、图表 img 真实加载（naturalWidth）、超高图占位 |
| 实机链路 | 浏览器表单提交一份洋河对公任务，全程监控到收尾 | 检索→抓取→摘要→报告→打包→反思→重做→收尾装配→交付 |
| 修订路径 | `POST /api/task/<id>/review/edit`（执行者发起的诚实修订，版本史留痕） | 重装配、重验收、投影同步、结构版本绑定 |
| 导出 | `/api/task/<id>/pdf` 真实端点 + `report_pdf` 结构检查器 | 页数、图嵌入/占位/超尺寸、表格列网格 |
| 离线 | 三场景 `scripts/scenario_run.py` 复跑 | 正常/亏损/缺附注三类路径的产物清单与形态 |

## 3. 分钟级运行报告

### 3.1 服务与代码装载

| 时刻 | 事件 |
|---|---|
| 11:23:43 | 本会话开始时 16/16 服务在跑（装载 `ebd2ac5` 时代码，含 C1/C2/C3） |
| 11:41:40 → 11:42:35 | 停服 → 依赖自检恢复便携 Redis → 启动（装载版本号口径修复 `4aee354` 的 web_ui 改动） |
| 11:49:09 | `4aee354` 提交；11:49:26 CI 起跑 |
| 11:49:48 | 提交实机任务（此时前端 dist 已含 `/files` 逐段编码与超高图占位） |
| 12:19:29 | `4dc1742`（装配幂等）提交；12:20:27 服务重启装载 |
| 12:22:22 | 修订重验（跑在幂等修复后的服务上） |
| 12:29:14 | 服务重启装载包陈旧信号 `29ec71d` |
| 12:30:23 / 12:31:51 | `29ec71d` / `c18d584` 提交；12:31:57 CI 起跑（覆盖三笔） |
| 13:11:52 | `d316a25`（标题行排除补修）提交；13:11:5x CI 起跑 |

### 3.2 实机任务时间线（11:49:48 – 12:05:47，共 15 分 59 秒）

| 时刻 | 事件 | 证据 |
|---|---|---|
| 11:49:48 | 任务创建（SUCCESS_WITH_ISSUES 前身，6 步模板计划） | DB `created_at` |
| 11:49:49 | 记忆注入 970 字符；Prompt RAG 0 条 | 任务日志 |
| 11:49:51 | 结构化数据源命中（2 份年报）；路由到确定性模板；**Critic 降级**（模板计划未评审） | 任务日志 |
| 11:49:51 → 11:50:32 | step 1 web_search SUCCESS（41s） | 任务日志 |
| 11:50:34 → 11:50:35 | step 2 / 2b web_fetch SUCCESS（含抓取夹具回放） | 任务日志 |
| 11:50:36 → 11:55:35 | step 3 content_summary SUCCESS（4 分 59 秒，最长一步） | 任务日志 |
| 11:55:36 → 11:56:31 | step 4 report_generator SUCCESS（55s）→ 验收内装配出候选稿 | 任务日志 + 版本 `4fd96fbc`(11:56:31) |
| 11:56:31 → 11:56:34 | step package SUCCESS → `deliverables_20260921_115634.zip` | 任务日志 + zip mtime 11:56:34 |
| 11:56:36 | 反思候选稿登记（`da62d9e4`，4646 字符） | 版本史 |
| 11:57:59 / 11:59:22 | 编排器侧两次生成调用（见 §3.4）：一次 ok、一次输出上限耗尽 | `llm_calls.jsonl` |
| 12:00:13 | 反思开始（"reviewing deliverable against goal"）；同刻主端点调用耗尽、**切备端点**再次耗尽 | 任务日志 + `llm_calls.jsonl` |
| 12:00:48 | 反思完成（36s）；12:00:49 单轮重做上限 2 步，重做 step 3 | 任务日志 |
| 12:00:49 → 12:04:45 | step 3 重做 SUCCESS（3 分 56 秒） | 任务日志 |
| 12:04:46 → 12:05:44 | step 4 重做 SUCCESS（58s）→ 重做稿 `eb4efbcb`(3067) | 任务日志 + 版本史 |
| 12:05:44 | 编排器判定"重做后 best_report 未改善（4646 -> 3067 字符），提前终止反思" | 任务日志 |
| 12:05:46 | 最终装配第一次（`82c6fd9a`，5411） | 版本史 |
| 12:05:47 | 验收修正 + 最终装配第二次（`17ca7ebc`，5533）→ 交付记录 ok | 版本史 + `deliveries` |
| 12:05:47 | 任务 completed（`completed_at`）；终态 SUCCESS_WITH_ISSUES | DB |
| 12:22:22 – 12:22:23 | 执行者发起的修订重验：新版本 `fde2f2e9`(5472) 采纳 + 干净重装配候选 `8b23aad3`(4940) | 版本史 + 交付记录 |
| 12:22:23 | 任务状态 SUCCESS/verified（DB `updated_at`） | DB |

### 3.3 版本史（7 版，全部保留）

| 时刻 | 版本 | 长度 | 采纳 | 说明 |
|---|---|---|---|---|
| 11:56:31 | `4fd96fbc` | 5255 | 当时采纳 | 报告步骤验收内装配（含 ③ 未证风险 ×3、⑦ 待核查块 ×1） |
| 11:56:36 | `da62d9e4` | 4646 | — | 反思候选稿（原始草稿，无装配块） |
| 12:05:44 | `eb4efbcb` | 3067 | — | 重做稿（分析节 891 字，**未进交付**） |
| 12:05:46 | `82c6fd9a` | 5411 | — | 最终装配 #1（回食 #1 的渲染产物：证明 `****`×3、"另有"×1） |
| 12:05:47 | `17ca7ebc` | 5533 | 当时采纳 | 验收修正 + 最终装配 #2（放大：`****`×6、"另有"×2） |
| 12:22:22 | `fde2f2e9` | 5472 | **现采纳** | 执行者修订（移除⑦块误收标题）+ 验收修正 |
| 12:22:23 | `8b23aad3` | 4940 | — | 幂等修复后的干净重装配（风险 4 条、`****`×0、⑦块×0） |

### 3.4 LLM 调用形状（工作区 `llm_calls.jsonl`，仅编排器侧 5 条）

| 时刻 | 阶段 | 尝试 | 耗时 | 输入字符 | 输出上限 | 结束原因 | 端点 |
|---|---|---|---|---|---|---|---|
| 11:56:42 | （空） | 1 | 6.1s | 2196 | 4096 | ok | primary |
| 11:57:59 | （空） | 2 | 46.3s | 2270 | 8192 | ok | primary |
| 11:59:22 | （空） | 2 | 55.2s | 2148 | 8192 | **exhausted**（empty_content） | primary |
| 12:00:13 | （空） | 2 | 50.7s | 2148 | 8192 | **exhausted**（empty_content） | **backup** |
| 12:00:48 | plan | 1 | 35.5s | 7546 | 8192 | ok | primary |

**上限核对**：落盘台账只有 5 条（编排器进程记录；worker 进程的检索/摘要/报告生成调用未进该台账），
**总调用次数不可确知**——40 次调用上限无法逐条核对。可核对的读数：5 条全部 ≤8192 输出上限、
无 4xx/5xx、两次以 `exhausted` 结束（主/备端点各一次）。

### 3.5 预算与端点

- 预算保持 0/0/0（`budget_state.json` 全零，与全局策略一致）。
- 任务提交前探针：primary / backup 均 `ok`（`llm_client.get_balance_status()`）。
- 费用：不可读，记"未知"。

## 4. 浏览器视觉测试记录

| 时刻 | 动作 | 结果 |
|---|---|---|
| 11:30–11:42 | 打开旧任务 `ui-f00775cfdf` 报告页（DOM 读数，无截图：该标签页截图面超时） | 发现"两个当前版本"（65ec49d1 vs 20719ee1）与图表全裂（`%2F` 404） |
| 11:43:29–11:43:53 | 首页 + 面板截图（js_90–js_93） | 面板区块顺序正确；**三张图 naturalWidth=0** |
| 11:46:21 | 滚动触发懒加载（js_109） | chart_1/3 恢复渲染；chart_2 读到 1038×**121366** |
| 11:47:46 / 11:47:53 | 占位改造后复测（js_117/js_118） | chart_1/3 正常、chart_2 显示"图片尺寸异常（1038×121366，高宽比 116.9）…" |
| 12:2x–12:3x | 新任务页面（DOM 读数；两次截图因浏览器通道失败改为文本核对） | 绑定警告 / 机器验收 / 人工待复核 / 包陈旧提示 四行读数 |
| 13:1x | 新任务图表复测 + 截图 | 6/6 图加载（各 1038×644）、无占位；附录图表带"问句+观察"图注与轴标题 |

截图失败两例（"surface preparation timed out"、"activity capture failed for guest"）已如实记录，
改用 DOM 文本读数兜底，未用"通道失败"掩盖结论。

## 5. 已修复的五个问题

1. **同页两个"当前版本"**（复核栏正文版号 vs 导出行谱系标识）。
   修复：`web_ui._export_payload` 同时给 `package/current_body_version_id`；面板按正文版号提示（`4aee354`）。
   复测：导出行显示"包生成于 cc6f2190bac2，当前版本 65ec49d1215e"，与复核栏同口径。
2. **正文图表全部 404**（整段 `encodeURIComponent` 把 `charts/` 编成 `%2F`，路由不解码）。
   修复：`/files` URL 逐段编码（`4aee354`）。复测：新旧任务共 9 张图全部 `loaded:true`。
3. **超高画布页面隐形**（1038×121366 缩放后 ~3.6px 宽）。
   修复：与 `chart_qa.MAX_CANVAS_ASPECT=12` 同阈值的如实占位 + 源文件可下载（`4aee354`）。
4. **装配不幂等**（两轮装配回食自身渲染产物：`****`、重复风险、"另有 N 条"自指、待核查头与标题成风险/主张）。
   修复：行尾加粗残留剥离、跨来源去重、装配器自身行按键排除、主张/未核查提取跳过标题行（`4dc1742`、`d316a25`）。
   复测：离线三场景 risks 5/4/7 条零伪影；**实机正文**（`17ca7ebc`）复跑风险提取得 4 条干净条目（此前 5 条含标题）。
5. **清单重写未重建 zip**（12:23 的清单配 11:56 的包，版本号对得上却说"包是当前的"）。
   修复：`package_stale`（清单比 zip 新 >120s）如实标注（`29ec71d`）。
   复测：payload `stale:true`，面板显示"重装配未重建包，包可能不含最新修订"。

## 6. 遗留项收口（第二轮：服务重启后复测，14:08 装载新代码）

### 6.1 装配候选未被采纳 → 机制查清 + 页面如实标注

**机制（代码级）**：修订路径 `POST /review/edit` 先对"用户编辑稿"跑验收并采纳（`交付正文采用验收修正版`），
随后 `assemble_and_verify` 登记代码装配候选并采纳它，但最后 `ensure_body_accepted` 拿到的
`accept_fn` 是**该次请求预先算好的验收结论**（lambda 返回旧 verdict）——于是它把"验收修正版"
重新采纳为交付正文（`交付正文采用验收修正版`），装配候选 `8b23aad3` 留档不交付。

**判定**：这是**设计语义**（用户修订不得被重建覆盖，C2-6 的要求），不是缺陷；被丢弃的是"候选稿更干净"
这一期望，而不是用户编辑。收口动作是让页面说对话，而不是绕过语义去改采纳规则：
- `_research_payload` 新增 `structure_version_exists` / `structure_version_adopt_reason`（读版本库，
  区分"绑定的是装配候选"与"绑定的是更早版本"）；
- 面板文案分三支：绑定未知 → "修订后需重新装配"；绑定代码装配候选 → "两者不同版：面板结论按该
  候选稿生成，勿直接当作交付正文的结论"；其他 → 原提示；
- 测试：`TestStructureBindingProvenance`（2 项）+ 前端守卫 `test_panel_distinguishes_binding_provenance`。
- 实机复测（14:1x，DOM 读数）：`· 面板（发现/缺口/证据）绑定的是代码装配候选（8b23aad32cf1），
  当前交付版本 fde2f2e9df1e——两者不同版：面板结论按该候选稿生成，勿直接当作交付正文的结论`。

### 6.2 评审降级 → 确认设计 + 补页面一行（含所属版本）

**设计确认**：`_require_review_or_refuse(task_id, "路由模板计划未经过 Critic 评审", plan=steps)`——
模板/路由计划不经 Critic；个人模式记 `DEGRADED` 放行并如实标注，银行模式拒绝继续（M0-b）。
已有 `test_review_protocol` 覆盖该策略；本轮实机 `review_state.json` 读数为
`verdict=DEGRADED / required=false / report_version_id=28a89aab…`。

**补的是什么**：该结论此前只写在交付正文抬头的引用块里，页面**没有任何一行**。现在
`_research_payload` 输出 `plan_review{verdict,label,degraded_reason,required,report_version_id,bound}`，
面板在"修改与重验"块渲染一行；`bound=false` 时明写"属版本 X，当前版本无评审结论"（修订后新版本
不继承旧评审）。测试：`TestPlanReviewProjection`（2 项）+ 前端守卫 `test_panel_shows_plan_review_row`。
实机复测：`· 计划评审（Critic）：未完成（降级）——路由模板计划未经过 Critic 评审（属版本
28a89aab9a95，当前版本无评审结论）`。

### 6.3 新闻稿被归类为"财务附注" → 按来源修好并在实机文档上复验

**根因**：C2-3 的降级规则（"命中变化关键词但无因果语言 → 不当变化解释"）把降级目标写死为
`KIND_NOTES`（财务附注）。实机 21 财经业绩说明会报道的前 400 字命中 `change` 关键词 3 次
（业绩说明会/营收/净利）、无因果语言 → 被标成"财务附注"。

**修复**：降级目标按来源分——发行人文件（`issuer_annual_report`）→ 财务附注（数字出处）；
第三方（新闻/解读）→ 业务背景；来源未知（旧调用形态）沿用附注（向后兼容）。
`classify()` / `_prose_classify()` 新增 `source` 参数，三个调用点传 `source_type()`/`document_provenance()`。
测试：`TestChangeDemotionTarget`（3 项）。

**实机复验（同一份抓取文档，只读）**：修复前 `footnote/财务附注`；修复后
`business_background/业务背景`（段落 1，字符 0-2850）；发行人年报样本仍 `footnote`；
`classify(默认)` 仍 `footnote`、`classify(source='third_party')` → `background`。
冻结的实机产物 `narrative_evidence.json` **不重写**（它是当时交付的证据，历史保留）；后续运行按新规则分类。

### 6.4 Mimosa 完整扫描（scanner_enobufs 收口）

- 扫描：deep，2026-09-21 14:08:02 起、14:08:16 完成；`scanId=scan-2026-09-21T06-08-16.249Z-3bee2b62df1d`，
  封印 `sha256:3d2b11b057cfc92914034c6a78cb2af7e142bbbff2e79422064b966b38d7addc`。
- 发现 **46 条**（36 high / 1 medium / 9 low）：路径穿越 17、SSRF 15、不安全随机数 9、
  硬编码凭据 2、其他 3（含 `distill 是 path-traversal 入口`、疑似跨文件污点、XML 实体扩展）。
- 覆盖率自述：`completeness=partial`、`runStatus=inconclusive`、威胁建模 `partial`（0 入口/0 主体）、
  验证阶段 `investigated=0`；`evidenceBoundary=static_only_no_runtime_execution`、`verdictEffect=none`。
  依赖：扫 295 个包，离线库命中 1 条告警。
- 人工核实（本轮）：
  - 2 条"硬编码凭据"均为**误报**——`frontend/src/components/Login.tsx:95` 是
    `autoComplete={isSetup ? 'new-password' : 'current-password'}` 的自动填充提示；
    `scripts/check_secrets.py:55` 是检查脚本里解释误报过滤的**注释**。
  - 抽检 `web_ui.py:303`（路径穿越）：`os.path.abspath(SHARE_FILE)` 用的是模块常量，非用户输入 → 误报。
  - 抽检 `workers/web_fetch_worker.py:89`（SSRF）：同文件第 77 行先过
    `adapters.transport._validate_public_url`（环回/私网/链路本地拒绝）才 `urlopen` → 已有守卫的调用点。
- **不宣称项目安全**：46 条中 2 条已核实为误报、2 条抽检为误报/已守卫，其余 42 条**未逐条验证**
  （扫描自述 partial/inconclusive）；后续要么逐条复核，要么在 CI 里加守卫可见的白名单机制。

### 6.5 仍存疑（未动，交决策）

1. **双装配链条的取舍**：报告步骤的验收会装配一次候选稿（设计如此，避免模型被要求补装配器本该写的东西），
   收尾的 `ensure_body_accepted` 又对同一份（可能已装配的）正文再装配一次——**第二次装配就是回食现场**。
   幂等修复后第二次装配不再产生伪影，但"是否应避免对已装配稿二次装配"仍是设计问题。
   证据链：`82c6fd9a` 的内容特征（尚不能证明 3→6、待核查主张 1→2、另有 0→1）只能由"输入含装配块"解释；
   代码路径 `research_candidate_body`（验收内装配）→ `ensure_body_accepted`（返回 accepted 作为 detail）→
   `assemble_and_verify`（再装配）。
2. **反思重做稿未进交付**：重做稿 `eb4efbcb`（3067 字符，分析节 **891 字**）比交付所用稿的分析节（214 字）
   信息更多，但编排器按**总字符数**判"未改善（4646 -> 3067）"而弃用。长度不是分析质量的代理指标，
   是否改为按验收结论/分析节长度比较，需决策。
3. **分析节只有 214 字的两条成因**（均可复现）：
   (a) `_analysis_section` 的 `_DUPLICATE_SECTIONS` 含"关键数据""核心指标"，模型自写的
   "二、关键数据一览""三、核心指标两年对比与逐项解读"被**整节丢弃**——后者实际是同比解读正文；
   (b) `llm_calls.jsonl` 显示反思期两次生成以 `exhausted`（输出上限 8192、empty_content）结束。
4. **新闻稿被归类为"财务附注"**：新任务 `narrative_evidence.json` 的 2 条定位中，一条把 21 财经报道的
   段落 1 记为 `kind=footnote`（财务附注）；同一文章的摘要含"2024 年经营计划 5%～10%"目标文本。
   该来源本次**被采用**（旧任务中它因"期间未标注"被排除），因模型分析仅 214 字而未形成目标类主张。
   准入分类的精确性（新闻 vs 附注）与"目标类文本来自已采用来源"的后续处理需复核。
5. **调用台账覆盖面**：worker 进程的 LLM 调用未进 `llm_calls:<root>` 台账（5 条仅编排器侧），
   使"≤40 次调用"这类预算上限无法事后核对——取证能力缺口。

## 7. 复测记录（针对不确定事实）

| 不确定事实 | 复测方式 | 结果 |
|---|---|---|
| 最终装配用了哪份正文 | 逐版标记计数（`风险与核查/尚不能证明/待核查主张/另有`）+ 代码路径核对 | 确定为"验收内装配的候选稿"，二次装配放大；反思两稿均未入交付 |
| 214 字分析的成因 | 对 6 份版本正文跑 `_analysis_section`（确定性）+ 节名关键词比对 + 读 `llm_calls.jsonl` | 双成因：(a) 节名过滤丢"核心指标两年对比与逐项解读"；(b) 两次输出上限耗尽 |
| 幂等修复在实机正文上是否成立 | 把 `17ca7ebc` 原文喂给修复后的 `_risks`（只读） | 首轮剩 1 条标题伪影 → 补修（全部标题行排除）→ 复跑 4 条干净、伪影 0 |
| 新任务图表是否受 `%2F` 修复影响 | 浏览器读 6 张 img 的 src/naturalWidth | 6/6 加载，URL 形如 `/files/ui-31305a2b28/charts/chart_1.png` |
| PDF 页数与图数 | `report_pdf` 结构检查器 + `/Type /Page` 计数 | 8 页、6/6 图在页、0 占位、0 超尺寸、1 表对齐 |
| 调用次数是否超上限 | 读 `llm_calls.jsonl` + 查记录覆盖面 | 台账仅 5 条（编排器侧）；总次数不可确知，如实记 |

## 8. 验证读数汇总

- **验收**：`overall=pass`、0 缺口；`rules_version=2026.09.12`、`rules_fingerprint=2ddecc9a`；
  正文 `report_sha256=fde2f2e9…`；交付 `report_version_id=a3eb6c32…`；manifest `verified`、
  `template_version=default`、`renderer_version=report_pdf/v1`。
- **来源**：采用 2 条 / 审计 4 条；叙事证据定位 2 条、缺类 `[经营变化解释, 风险因素]`；`rules_version=f3a-1`。
- **面板（DOM 实测，两轮）**：绑定来源说明（装配候选 `8b23aad3` vs 交付版 `fde2f2e9`）、
  机器验收 pass（绑定本版）、计划评审（Critic）降级行（属版本 `28a89aab…`，当前版本无评审结论）、
  人工复核待复核、包陈旧提示——四/五行全部渲染正确。
- **离线场景**：normal_growth verified（证据 4、缺口 0、图 5）；loss_mixed_units / missing_footnote_asof
  按契约 draft；risks 5/4/7 条，无 `**` 残留、无重复、无装配器伪影。
- **测试**：`test_delivery_chain` 252、`test_frontend_guards` 44、`test_narrative_evidence` 38、
  `scenario_checks` 17、`test_offline_delivery` 32 全绿；前端行为测试 0 fail；`npm run build` 通过。
- **安全扫描（Mimosa，deep）**：46 条静态发现（36 high/1 medium/9 low），封印
  `sha256:3d2b11b0…`；覆盖率 `partial`/`inconclusive`；2 条凭据发现核实为误报 + 2 条抽检
  （误报/已守卫），其余未逐条验证；**不宣称项目安全**。
- **CI**：`4aee354` run 35558815699（11:49:26→11:57:18）success；`c18d584` run 35561348419
  （12:31:57→12:40:08，覆盖 `4dc1742`/`29ec71d`）success；`d316a25` run 35563702160
  （13:11:57→13:19:23）success；收口笔见 §6 与推送记录。

## 9. 未验项与边界

- 采纳正文 `fde2f2e9` 仍带旧渲染伪影（历史版本如实保留）；干净重装配候选 `8b23aad3` 按修订路径
  语义留档不交付（§6.1）——面板已按"装配候选留档"如实说明，不再误报"需重新装配"。
- 评审未完成（降级）：模板/路由计划不经 Critic（设计），页面已有独立一行；交付物须人工复核后方可使用。
- 分析节 214 字（§6.5）：节名过滤 + 输出上限耗尽双成因，按"不编造"原样呈现。
- 调用台账只覆盖编排器侧（5 条），总调用次数与费用不可确知——"≤40 次调用"无法事后逐条核对。
- Mimosa 46 条静态发现中 42 条未逐条验证；不宣称项目安全。
- 研究员五项 0–2 评分未做（需真实研究员；不得由执行者填写）。
- 银行可用性、外部发布未获验收。
- 本报告不含任何密钥、账号或完整客户提示词；调用台账只含形状字段。
