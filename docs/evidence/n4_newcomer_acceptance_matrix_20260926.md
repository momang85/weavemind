# N4：新人路径验收矩阵（09-26）

**指令**：`docs/新人一键启动与首次研究体验_20260925.md` §6。
**验收对象**：发行候选包（`weavemind-2026.09.26-win-x64.zip`）与正常页面。
**本轮范围**：只有开发机可用（Windows 10/11 x64，**有** Python 3.14 + Node 24，**无** Docker）。
因此矩阵里"干净环境"相关行**如实标为未验**，不写成通过；能在这台机器上真跑的项逐条附证据。

> 本批**未**新增任何付费模型调用；需要真实模型的端到端项留待显式授权（见文末）。

## 1. 矩阵

| # | 场景 | 状态 | 证据 / 说明 |
|---|---|---|---|
| 1 | 干净 Windows、无 Python/Node/Docker | **未验** | 需要一台**没有**系统 Python/Node/Docker 的机器：解压双击进入引导、页面填必要信息、无手工命令。候选包与构建说明已就绪（`docs/新人运行包构建说明.md`）；本机不具备该条件 |
| 2 | 已有不兼容 Python / pip 源污染 | **部分** | 运行包自带 **3.11.9** 独立运行时：本机系统 Python 是 **3.14**，包内验证用的是包自己的 `runtime/python.exe`（3.11.9 / OpenSSL 3.0.13 / SQLite 3.45.1），不读系统解释器与 site-packages。未验：真机上 pip 源被污染且系统 Python 不兼容的整链 |
| 3 | 中文与空格路径、移动解压目录 | **通过（本机）** | 见 §2.1：解压到 `…\织光 运行包 验收 2026\解压目录\` 后包内解释器导入正常、运行身份识别为 `package:2026.09.26`、渲染 1 页中文 PDF 并可提取中文；绝对路径扫描 0 命中 |
| 4 | 网络断开、下载损坏或中断后重开 | **未验（真机断网）**；离线语义已受控验证 | 便携 Redis 随包提供离线复用入口（摘要与参考值一致）；`dep_check` 的多源预算/摘要固定/离线复用与 launcher 的断点恢复（身份未变即跳过已验证步骤）有行为用例；未做真机断网演练 |
| 5 | Redis 5 / 端口冲突 / 自定义端口 | **部分** | 自定义端口：`web_port()` 是唯一来源，就绪探测打在实际端口（行为用例）；Redis 兼容下限与"不接管非本项目实例"是既有实现。未验：真机 Redis 5 与端口被占用 |
| 6 | 连续双击、已有本实例运行、部分服务起不来 | **通过（本机）** | 见 §2.2：第二次启动复用实例、16 个 PID 未变；spawn 失败计入 total/down；未就绪时不打印"可研究" |
| 7 | 无密钥 / 错误密钥 / 角色模型不支持 | **部分** | 演示与真实分开：内置演示只读、常驻"非本次实时生成"标注、不写入统计（N3 通过）；错误在**原页面**纠正走既有设置页，连通性测试由用户点击触发。未验：真实错误密钥的端到端（需付费端点） |
| 8 | 无 Docker 的公司研究 + 明确代码任务 | **通过（公司研究，本机）／部分（代码任务）** | 公司研究在无 Docker 机器上跑通（前批实机与冻结样本）；代码任务：**不自动降级**（`restricted/none` 需操作者显式选择），启动输出、依赖自检与修复轮跳过都给出路。未验：真机跑一个含 `code_execution` 的任务看提示（本批未跑，避免无谓耗时） |
| 9 | 修订、导出、退出后重开/升级 | **通过（本机）** | 前批页面整链：修订 → 采纳 → 同版导出 → 下载与包内字节一致；本批重启后账户/配置/历史保留（会话存库、实例复用），旧批准不迁移有既有用例 |

## 2. 本机实测记录

### 2.1 场景 3：中文与空格路径（含中文 PDF）

```
解压目标：C:\Users\ding0\AppData\Local\Temp\织光 运行包 验收 2026\解压目录\weavemind-2026.09.26-win-x64
包内解释器：runtime\python.exe
  → import launcher, task_intent, dep_check, web_ui 全部成功
  → launcher.web_port() = 8080
  → launcher.runtime_identity() = package:2026.09.26（包清单被识别，断点恢复按包版本失效）
  → report_pdf.markdown_to_pdf 渲染中文：1 页、5,193,040 字节、嵌入字体（FontFile/FontName/ToUnicode）
  → 提取文本含「营业收入 / 288.76 / 331.26 / 中文自检 / 指标」
```

说明：这一步用**包内解释器直接调用**（等于绕开 `start.bat`），所以它验证的是**运行时与路径可迁移性**，
不是双击入口本身——双击链路的验收属场景 1（未验）。

### 2.2 场景 6：连续双击与实例复用

```
第二次 `python launcher.py up` → ［OK］已在运行（16 个服务）：http://localhost:8080——已复用本实例
前后 PID 逐项相同（critic/webui/orchestrator/… 16 项），failed: []
三层状态：工作台 HTTP 200 @8080 ／ 研究能力就绪（Redis 8、5 项必需 Worker 心跳新鲜）／
          代码执行：容器隔离不可用（docker 不可用）——涉及代码执行的步骤会被拒绝
```

### 2.3 人工命令数与操作次数（本机能测的部分）

| 项 | 值 |
|---|---|
| 新人主路人工命令数（本机代跑） | 启动 1 条（等价于双击入口内部调用）；**配置模型与账户除外** |
| 首次填写信息 | 模型服务/密钥/模型 + 管理员账户（**不伪称零配置**，这两类信息无法代办） |
| 本批付费请求数 | **0**（未跑任何真实模型任务） |
| 候选包体积 | zip 291.7 MB ／ 解压 ≈1.1 GB（含 108 个运行依赖） |
| 本轮下载量 | 运行时 11.2 MB + 依赖 wheel（构建期）；新人首启若用随包 Redis 压缩包则无需联网取 Redis |

## 3. 需要补的条件（交给后续）

1. **一台干净 Windows**（无 Python/Node/Docker、非开发者账户）：跑场景 1 与场景 2 的整链；
   记录解压→双击→引导→首个任务的实际耗时与点击次数。
2. **真机断网演练**（场景 4）：解压后断网启动，确认停在明确的恢复动作而不是无限换源。
3. **Redis 5 与端口占用**（场景 5）：装一个 Redis 5 与一个占用 6379/8080 的进程，
   确认兼容性判断与"不接管他人服务"。
4. **错误密钥端到端**（场景 7）与**含代码任务的提示**（场景 8）：需要一次真实模型调用才能覆盖；
   按指令"本小批不为证明一键新增付费研究"，留待显式授权与既有预算。

## 4. 本轮不做

- 不发布 Release、不上传任何数据、不改用户 `config.json` 与 `templates.json`；
- 不为了凑"全部通过"而把未验项写成通过；场景 1/2/4/5/7/8 的未验部分在 §1 逐条写明。
