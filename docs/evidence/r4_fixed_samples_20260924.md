# R4 证据（09-24）：固定样本回放与指标记录

**指令**：`docs/阶段D系统收敛与研究效能提升_20260923.md` §7（R4）
**产物**：`docs/evidence/r4_fixed_replay_20260924.json`（逐任务指标与核对）；
`evals/scenarios/r4_*.json`（预期在实现前冻结：三个新场景）；`scripts/r4_replay_fixed_samples.py`。
**范围**：无新增 HTTP、无付费模型；全部离线（模拟/冻结输入）。

## 1. 固定样本（输入与预期先冻结，事后不改）

| 样本 | 类型 | 冻结预期 | 实测 |
|---|---|---|---|
| `r4_sufficient_second_company`（伊利股份 600887.SH） | 非金融·资料较充分（含销售量/库存量与分产品占比） | 交付 verified、必需事实不缺、跨章节矛盾 0、模型请求 0、收入问"分解覆盖（部分）"、正文含『关键判断与下一步/逐问题资料计划/量价与结构/分别闭合』 | **pass**（delivery=verified，acceptance=pass，contradictions=0，facts_missing=0，requests=0，revenue=partial） |
| `r4_insufficient_evidence`（同一公司·缺 2024 现金流、无正文） | 资料不足 | 缺口 ≥3 且**照常出稿**、正文含"原因待证/缺口/关键判断"、模型请求 0 | **pass**（delivery=draft 如实、missing_labels=3，requests=0） |
| `r4_bank_subject`（招商银行 600036.SH） | 银行作为**研究对象**的适用性边界 | 交付 verified、企业口径比率（净利率/现金覆盖/资产负债率/研发强度）**不生成**、正文含"金融机构/不呈现"、请求 0 | **pass**（verified，无企业口径比率，含适用性说明） |
| `ui-706c5ef4a5`（洋河，用户修订样本） | 经页面同路径人工修订的采纳版 | 旧批准不迁移、同版导出、修订后模型请求 0、交付绑定采纳版 | **pass**（评审记录仍绑旧身份 `60781cdc…` ≠ 当前采纳；包身份=采纳身份；修订后无新请求） |

汇总（`r4_fixed_replay_20260924.json`）：**4/4 pass**，逐任务耗时 6–11 秒（离线装配+渲染+导出）。

## 2. 三层测试

1. **纯函数正反例**：`test_question_assessment` 语义矩阵（并入 `test_narrative_evidence`）、
   `test_report_quality.TestContradictionDetector`（9 例：水平/变化、跨年度、真矛盾、表格行、
   长短语归属、实物量、别名、四舍五入、写法差异）、`TestQualityVectorOrder`（3 例）。
2. **整链回放**：`scripts/scenario_run.py`（装配→验收→版本→导出→PDF/清单），
   9 个场景（6 旧 + 3 新）全通过；`scripts/r4_replay_fixed_samples.py` 汇总指标。
3. **正常页面**：采纳/修改/重验/重包在 R2 批次用真实页面按钮跑过（`aac5d641`→重包 `3d22ce`）；
   R3 采纳走**页面按钮背后的同一套函数**（`scripts/adopt_candidate_page_path.py`，
   因服务重启后浏览器会话失效、无凭据，未重复点按钮——如实记录）。

## 3. 固定输出验收（逐项）

- **严重错主体/错期/错引用/虚构归因 = 0**：`wrong_subject_period`（错主体/错期材料被排除）、
  `attribution_support` 与 `source_labeling` 在各场景均 pass；银行样本不套用一般企业解释。
- **关键判断与必需数字可定位/可复算**：`facts_missing=0`（充分样本与不足样本）、
  溯源率与金额溯源率见各场景 manifest（充分样本 financial 域 pass）。
- **跨章节自相矛盾 = 0**：新检测器（保守：水平/变化分开、期间进键、长短语优先归属、
  实物量与表格行跳过、百分比零头容差）在 9 个场景中 7 个为 0，`margin_mix` 3、`all_decline` 2
  ——剩余计数是**单位混用场景**下的保守提示，作为候选比较的计数项，不参与验收结论（如实记录）。
- **内容丢失 = 0**：包内 PDF 与正文逐句核对（`scripts/pdf_package_check_20260923.py`），
  R3 版本 14 页 0 越界、标题与关键句全部可找到。
- **旧批准不迁移**：用户修订样本实测（评审记录版本 ≠ 采纳版本）。
- **同版导出或明确冲突**：包清单身份 = 采纳身份；快照绑定 `binding_verified=true`
  （检验失败即 409，见 R2）。

## 4. 逐任务指标（明细见 JSON）

| 样本 | 耗时(s) | 模型请求 | 缓存命中 | 人工关键修订 | 复核时间 | 再次使用意愿 |
|---|---|---|---|---|---|---|
| r4_sufficient_second_company | 9.96 | 0 | 0（离线场景无缓存通道） | 0 | 未测（无评审者） | 未测 |
| r4_insufficient_evidence | 8.67 | 0 | 0 | 0 | 未测 | 未测 |
| r4_bank_subject | 5.99 | 0 | 0 | 0 | 未测 | 未测 |
| ui-706c5ef4a5（用户修订） | 0（只读核对） | 修订后 0 | 资料缓存：命中（`snapshot_sha256` 相同则不重建） | 1（人工修订一次） | 未测 | 未测 |

- 费用：**未知**（未调用真实供应商，无 tokens 可记）。
- "无模型的编辑/重验/重包 = 0 请求"：以 `llm_calls.jsonl` 的**最新请求时间 <  最近一次装配时间**
  验收（用户修订样本实测通过）。

## 5. 真人评分（准备就绪，不代填）

操作步骤与产物位置见 `docs/evidence/r4_scoring_handoff_20260924.md`（配套
《研究员评分表_F3_20260920》五项、建议 ≥8/10 且无严重问题）。当前状态：
**代码/产物关卡已完成，真人验收待做**——本轮没有评审者，未填写任何评分。

## 6. 未验项（如实列出）

- **真实 Redis 多进程、取消/重启一致性**仍未验（既有未验项；本轮未用模拟供应商做定向验证，
  留待下一批，避免在无隔离环境时触碰真实供应商）。
- 页面 UI 本轮未重复驱动（会话失效）；真实并发修订、跨进程并发导出未验。
- `r4_bank_subject` 的收入问覆盖为"无相关材料"（年报正文里"主要受净息差收窄影响"未被识别为
  收入问的管理层归因）——**记为后续改进点**，不影响本批"适用性"边界结论。
- 主文页数未达 2–4 页软目标（14 页），与 R3 记录一致。
