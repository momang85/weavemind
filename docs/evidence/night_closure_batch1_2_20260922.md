# 09-22 晚间收口复验：第一小批（确定性缺口）与第二小批（受限资料获取）

2026-09-22 晚。指令 `docs/阶段D收口复验与下一步_20260922.md`（审查 `8822acf`）。
**未追加付费实机、未换模型、未改全局 0/0/0、未改 templates/权限。**

## 第一小批：点名缺口逐条关闭（离线）

| 缺口（指令 §2） | 修复前实测 | 修复后实测 |
|---|---|---|
| 同主体跨指标同值仍被提升（归母净利率25%/资产负债率25%/现金覆盖率25%） | pass，溯源 100%（后两项被 `same_value_elsewhere` 提升） | **fail，50%（2/4）**；提升键改为「数值+符号+单位+**指标**+**期间**」，且 `semantics_unverified`/被提升项/已披露项**不得**充当提升证据 |
| 归因检查用全局存在条件（任意 `revenue_yoy` 即通过） | pass | **fail**（缺证归因）；支持必须**逐条对应**：该驱动要在已准入披露里被谈到，或底稿里有**对应**派生（材料词命中） |
| 比率检查用全局存在条件（任意 `cashflow_coverage` 即通过） | pass | **fail**（不可复算）；只有**该比率自己**的两期派生读数或句内两期读数才算可复算 |
| `chart_N` 别名错位漏检（chart_5 被说成现金覆盖） | 检查只认"图N"形状，整条漏检 | **fail，2 处不符**；新增 `chart_N` 识别 + 主题词匹配（本图用放宽形、他图只用非通用主题词），未知指向明示缺口 |
| 契约三问缺两项仍 ready 1/1 | ready，1/1 | **draft，1/3**；分母来自**契约**，缺条目按缺口记（写明"结构里没有这些必答问题的条目"） |
| 只改资料/规则指纹仍 current | True | **False**；`state_is_current` 增比资料指纹与规则版本；研究任务绑定信息缺失（契约/结构/资料任一为空）→ 未知/待重验；读取端 `_research_state_for` 传当前结构与**任务记录解析出的契约**（不回读旧 binding） |
| 异步 415 只有 1 条调用形状 | 1 条 | **2 条**（`stream_fallback_ok` / `_failed` 各一条；usage 计算不变） |
| 账本初始化未检查锁 got | 锁返回 False 仍写 1 次、生成 ledger_id、persist_uncertain=False | **不写、不生成身份、标记不确定**；有界多进程据此拒绝新付费请求（"待对账"） |
| PDF 空工件仍二次下载 | artifact 缺失 → `data=None` → `doc_from_url` 重抓 | **保留缺口**，不调解析通道（用例断言零调用） |
| 新稿 §5.2 分子/分母写反 | "分母降得更快" | **"分子降得更快"**（资产负债率＝负债/资产；负债是分子） |
| 图引用整体错位（chart_2 说成两年对比、chart_5 说成现金覆盖…） | 模型正文错位 | 候选修订按**真实 chart_id** 重映射 5 处（保留人可读图号） |
| PDF 机械核对式减号丢失（U+2212 显示方框） | 方框/丢失 | 数学符号归一（U+2212/全角连字符/各类空格 → ASCII），PDF 第 1 页公式可读 |
| 第 1 页 23.24% 断行、第 2 页孤立"图表"标题 | 存在 | 折行不切断数字 token；标题随下一块（图片按实际显示高度）——实测第 2 页不再留孤立标题 |

冻结用例：`test_delivery_chain.TestNightClosureCounterexamples`（6 例，含正例）、
`test_root_budget.TestNightClosureBudgetBranches`（3 例）。
`test_delivery_chain` 321 项全绿；`test_root_budget` 75 项全绿。

## 第二小批（部分）：受限资料获取（≤6 HTTP / ≤3 分钟，无模型）

脚本 `scripts/fetch_yanghe_ar2024_bounded.py`（走 `net_policy.fetch_document`，固定主机
`np-cnotice-stock.eastmoney.com`，art_code 形状校验，页间 1s）：

| 读数 | 值 |
|---|---|
| HTTP 请求 | **6**（上限 6） |
| 总耗时 | **7.4s**（上限 180s） |
| 产物 | `evals/real/yanghe_ar2024_pages_20260922.json`：art_code `AN202504281664011244`、披露日 2025-04-29、每页原始 URL / 真实页码 / **原始字节 sha256** / 正文 |
| 解析（页文本 → 带页码偏移的文档） | 30,005 字符，页码偏移 [(0,2),(5001,3),(10002,4),…] |
| 定位 | **7 条**已准入带定位记录，类别：业务背景 / 财务附注 / 风险因素 |
| 收入解释 | **第 3 页**含发行人原文："…中端和次高端价位段承压较大…积极调整经营策略…2024 年实现营业收入 288.76 亿元，同比下降 12.83%" |
| 利润/现金 | 同页只有**读数**（"归属于上市公司股东净利润 66.73 亿元，同比下降 33.37%"），**没有**对应的原因表述 → 仍按缺口（收入解释不得扩散） |

离线链读数见 `docs/evidence/_night_material_chain_20260922.json`（脚本
`scripts/verify_material_chain_20260922.py`）。**注意**：摘录重排的测试 PDF 只作解析夹具，
本批取的是公告文本 API 的真实分页正文（带原始字节 hash 与真实页码），未把夹具当页码实证。

## 未验项（如实，未做的不写成已做）

- **正常入口链未验**：未按现有启动方式加载本批版本，未走页面/HTTP 修订→状态→PDF→新 ZIP；
  服务仍装载修订前代码。原因：本轮上下文预算已用尽，且重启需按既有顺序
  （stop → deps --fix → start）——留待下一小批，不冒充已验。
- **主文收敛（第三小批）未做**：元数据外置（完整指令/bank_corporate/chart_id 绑定字典
  移出读者正文）、主文去重到 2–4 页、同版 ZIP 均未开始。
- 公开原文获取只做了**取页**这一环；抓取→准入→报告的正向全链（在真实任务里）未跑。
- 版面：第 1 页"资产负债率 23.24%"断行已修；第 4 页图注行尾数字断开等未逐一复核。
- 研究状态仍为 `research_draft`（必答三问 0/3 有定位依据）：这是如实结论。
