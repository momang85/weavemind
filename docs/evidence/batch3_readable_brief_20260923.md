# 第三批证据：交付一份真正能读的公司研究简报（2026-09-23）

指令：`docs/阶段D正常交付闭环执行指令_20260923.md` 第三批。全程无模型调用（修订走页面入口，重验/导出/打包都是确定性代码）。

## 1. 保留清单（先列再删）

**保留**：关键发现 4 条；研究问题与下一步 4 组（收入/利润/现金/银行视角，各带支持·边界·下一步）；
结论（模型 7.2 逐字）；数据时效（获取时间/币种/口径依据）；财务对照表；同比与比率 12 行；
图 1–3 与图注（主文）；风险与核查（含待核查主张、结论边界）；附录全部小节
（参考来源/资料范围与口径/字段位置与计算底稿/金额变化（可复算）/比率适用条件/
缺口清单（需补材料）9 项/其他核查项/其他图表 图 4–6/版本与验收状态/免责声明）。

**移出读者主文**：整段任务指令标题块、`bank_corporate` 术语、hash、chart_id、
"关键数据一览（汇总复核）"空节、模型稿里逐节复述（2.x–9.x）、投资建议复述、
"见 6.3 表"式失效引用。**没有机械截断**：每条被删段落的内容都能在保留清单里找到对应
（缺口 9 项压缩成"缺口清单（需补材料）"；数据时效与结论搬进主文）。

## 2. 候选与版本链（保留历史，不改用户字节）

`scripts/candidate_convergence_20260923.py`（离线生成候选，来源固定为完整稿 `37e0657a`）
→ 经**正常页面修订**逐次落库：

| 版本 | 来源 | 结果 |
|---|---|---|
| `37e0657a` | 上一批采纳稿（15,701 字符） | 基线 |
| `71c3886a` | 首版候选（7,743 字符） | 机器验收 **fail**（缺结论段、缺数据时效块）→ 交付为草稿，**版本保留** |
| `c1aa553d` | 补结论+数据时效（8,324 字符） | 验收 **pass** |
| `1a3477c5` | 修失效引用"见 6.3 节"→"附录『缺口清单（需补材料）』" | 验收 **pass**，当前采纳 |

## 3. 渲染修复（生产代码）

- `report_pdf._safe_break`：负号不留在行尾（实机 `（-` + `12.83%）`）；行尾收尾标点
  （。，、）】等）不留行首。
- `report_pdf.markdown_to_pdf`：标题先过 `normalize_math_symbols`——否则正文 H1 的
  en dash 被归一而 title 参数没有，去重判断失配、标题印两遍（实测 count 2 → 1）。
- `web_ui._task_pdf_bytes`：PDF 抬头改用**正文一级标题**（此前用整段任务目标，读者
  首页顶着任务指令）。
- `report_brief._save_full_model_report`：**按内容 hash 命名**（`model_report_full_<sha16>.md`），
  不再固定名覆盖历史；写完校验存在与 hash，失败返回空串；装配文本不再宣称"逐字保存在
  交付包内"（失败时如实写"未能落盘校验通过"）；`repack_adopted` 把 `audit/*.md` 收进包。

## 4. 正常入口链复验（同一任务 ui-706c5ef4a5）

| 读数 | 值 |
|---|---|
| 采纳正文 | `1a3477c5`；研究状态 `stale=false`（结构与采纳正文同源）、located 8、必答 2/3、20 条肯定结论未证实 → 仍 `research_draft` |
| 交付 MD | 8,454 字符（原 36,917），无任务指令、无 `bank_corporate`、无 chart_id；"见 6.3 节"已改为附录指向 |
| PDF | 9 页（原 15 页）；首页抬头为"洋河股份 经营分析简报（2023–2024 年度）"，标题仅 1 次；负号/标点断行修复 |
| 主文/附录 | 主文（标题→风险与核查，含 3 张图）约 5 页；附录 4 页（含图 4–6、底稿字段、缺口清单） |
| 新包 | `deliverables_20260923_014753.zip`：12 个成员（MD/PDF/底稿 json+csv/6 图/**audit 模型稿**/包内清单）；页面回报"包内字节自检 全部一致" |
| 清单 | schema `weavemind.package/2`；`report_version_id=be0255ec…`；`research_body_sha256=1a3477c5…` ≠ `delivered_md_sha256=6339e835…`；`pdf_sha256=a5687c87…`；`rules_version=2026.09.12` |
| 旧包 | 前两个 zip 保留（`deliverables_20260922_120247.zip`、`deliverables_20260923_011430.zip`），未改时间/标识 |

## 5. 未验项（如实）

- 主文 5 页（含 3 张图）**未达到 2–4 页目标**：已按问题组织、去重复、把底稿与次要图移入
  附录；再压缩需要取舍（例如把"同比与比率"12 行整体移入附录或合并图），本轮未做。
- 未做第二人视觉复核（visual-judge 子代理账号不可用）；本轮只逐页目视 1/3/5 页与首页文本抽取。
- 修订只走页面 `review/edit` 的"整份替换/查找替换"两条路径；未做并发修订的混版实测。
- `audit/model_report_full_*.md` 的**历史版本**（旧固定名文件若存在）未迁移；本轮包内是该版
  一份（`99b64229a88c1d7d`）。
