# 实机端到端验证与修复记录（2026-09-21，洋河对公任务 ui-31305a2b28）

触发：用户要求"发起全方位端到端测试，用视觉测试前端渲染"。基线 `ebd2ac5`（C1/C2/C3 已落），
本批提交 `4aee354`（任务页版本口径/图表渲染）、`4dc1742`（装配幂等）、本批第三笔（交付包陈旧信号）。

## 1. 运行事实（可复核）

- 任务：洋河股份 002304.SZ，A股，2023–2024，合并，银行对公视角，资料截至 2025-04-30。
- 提交 2026-09-21 11:49:48（本地），12:09 完成（约 20 分钟，在 25 分钟上限内）；
  终态 SUCCESS_WITH_ISSUES；修订重验后 SUCCESS/verified。预算维持 0/0/0；两端点余额 ok。
- 任务跑在 11:42 启动的服务（装载 `4aee354` 前工作区代码，含 C1/C2/C3 全部）；
  修订重验跑在 12:20 重启的服务（装载 `4dc1742`）。
- 版本史（report_versions.json，按时间）：4fd96fbc(5255,11:56:31 修正版) →
  da62d9e4(4646,候选稿) → eb4efbcb(3067,反思重写) → 82c6fd9a(5411,代码装配) →
  17ca7ebc(5533,修正版,首轮采纳) → fde2f2e9(5472,修订修正版,现采纳) →
  8b23aad3(4940,代码装配,干净候选,未采纳)。
- LLM 调用数：未产生 llm_calls.jsonl（该日志只在特定路径写），费用不可读——如实记"未知"。

## 2. 浏览器端到端发现的五个真问题与处置

| # | 现象（实测） | 根因 | 处置 |
|---|---|---|---|
| 1 | 同页"本版 65ec49d1"与"当前版本 20719ee1"并存 | 复核栏用正文版号、导出行用谱系标识 | `_export_payload` 同时给两命名空间，面板按正文版号提示（`4aee354`） |
| 2 | 正文三张图表全部裂图（naturalWidth=0） | 前端对整段相对路径 encodeURIComponent，`charts/` 变 `%2F`，路由不解码 → 404 | /files URL 逐段编码（`4aee354`），修复后 chart_1/3 正常渲染 |
| 3 | chart_2 为 1038×121366 历史异常画布，页面只剩 ~3.6px 宽 | 旧渲染产物；页面无纵横比防线 | 与 chart_qa.MAX_CANVAS_ASPECT=12 同阈值换如实占位，源文件可下载（`4aee354`） |
| 4 | 采纳正文风险区 `证明****`、同一风险两行、"另有 N 条"自指、待核查头与报告标题被当风险/主张；经两轮装配逐轮放大（0→3→6） | `_risks` 不剥 `**` 残留、不跨来源去重、不排除装配器自身行；`_claims`/`_unsupported_claims` 不跳过 H1 | `4dc1742`；离线三场景 risks 无伪影无重复；实机结构重写后 risks=4、claims=0、unsupported=0 |
| 5 | 修订重验重写 export_manifest（12:23）却不重建 zip（11:56）——按版本号比较会误称"包是当前的" | 打包与清单分属两步，重装配只写清单 | `package_stale` 信号 + 面板如实提示（本批第三笔） |

## 3. 验证读数

- 修订重验（执行者经 `/review/edit` 移除"⑦待核查块误收的报告标题"触发，版本史如实留痕）：
  机器验收 pass（绑定本版 fde2f2e9）、human_review.json 无记录 → 面板"人工复核：待复核"。
- 修订版 PDF（X-Report-Version-Id=a3eb6c32）：8 页、嵌入图 6/6 全部在页内、0 占位、
  0 超尺寸、表格 1 张列对齐（report_pdf 结构检查器）。
- 面板文本（DOM 实测）：绑定版本警告（8b23aad3 vs fde2f2e9）、机器/人工两行分开、
  包陈旧提示"重装配未重建包"。
- 离线场景三连（修复后代码）：normal_growth verified、loss_mixed_units/missing_footnote_asof
  按契约 draft；risks 5/4/7 条，无 `**` 残留、无重复、无"另有/待核查主张/标题"伪影。
- 测试：test_delivery_chain 246、scenario_checks 17、test_offline_delivery 32、
  test_frontend_guards 45 全绿；前端行为测试 0 fail；`npm run build` 通过。
- 截图证据（浏览器实拍）：面板发现/缺口/证据区、机器与人工两行、版本号一致的导出行、
  chart_1/3 渲染、1038×121366 占位。截图通道两次瞬时失败（guest capture），已用文字 DOM 读数兜底。

## 4. 未验项与遗留（如实）

- **编排层待决策**：反思重写稿 eb4efbcb（12:05:44）未进入最终装配——最终装配沿用了
  11:56 的修正版正文。哪个候选应进交付属编排语义，未擅改；版本史完整可查。
- 现采纳正文 fde2f2e9 仍带旧渲染垃圾（**** 风险等）；干净重装配候选 8b23aad3 存在但未被采纳
  （修订路径的语义是"采纳用户编辑并验收修正的那版"）。面板绑定警告如实显示该错位。
- 分析节 214 字（模型本次产出极短），按"不编造"原样呈现。
- 评审未完成（降级）：路由模板计划未经 Critic 评审，交付物须人工复核后方可使用。
- Mimosa scanner_enobufs：本批三笔提交均未取得完整扫描结论，不宣称项目安全。
- 研究员五项 0–2 评分未做（需真实研究员，不得由执行者填）。
