# 批次 B：资料契约穿过 Critic 到执行端（09-22 复核 P1 定向修复）

2026-09-22。指令 `docs/阶段D实机复核与下一批指令_20260922.md` §3-B（审查基线 `7ec2003`）。
全部**禁网**离线验证：只打桩传输层/模型回包，不联网、不调用模型、不追加实机。

## 1 复现（实机 ui-706c5ef4a5 的实际派发）

`logs/worker-search.log:33778–33819`：

| 证据 | 内容 |
|---|---|
| 派发指令 | **没有** `[检索查询]` 行（Critic 用模型修订计划时把那一行丢了） |
| 引擎实收 query | `2023 2024 2025 研究洋河股份 两个年度 营业收入 归母净利润 经营活动现金流净额 报表口径 截至 每个数字须能回溯到 位置 可重算 缺证据 如实标缺口 阅读重点 银行 对公客户研究视角 bank corporate`（样板 + 契约外年份） |
| 历史教训 | "明确要求搜索关键词必须包含'2025年三季报'……确保结果属于2025年三季度" |
| 历史经验 | 别的任务（宁德时代/腾讯/美联储）的 2025–2026 期间混入首步骤指令 |

结论：**可被模型删掉的一行提示不是契约**；契约必须是结构化字段，并在修订后重建、
派发时复核。

## 2 实现

| 位置 | 变更 |
|---|---|
| `execution_contract.py`（新） | `ExecutionContract`：主体/代码/市场/期间/口径/as_of/文档类型/必需指标 → 指纹、按年短查询、期间冲突判定、`apply_to_steps`（幂等重建）、`violations`（不变量）、`mark_inapplicable`（标注不适用） |
| `orchestrator_v2._fixed_research_plan` | 固定研究路线构建契约并按任务留存（`_task_contracts`） |
| `orchestrator_v2._review_plan` | 修订稿**先按契约重建**（剥旧 `[检索查询]`/`[研究契约]` 标记 → 重加当前契约）→ 校验不变量 → **复评评的是重建后的这一版**；重建后仍冲突 → 按"评审未完成"处置（不带着冲突期间拿 PASS） |
| `orchestrator_v2._dispatch` | 派发前复核契约指纹：不一致 → 按契约重建（记日志）；重建失败/未生效 → **拒绝派发**；研究步骤查询行含契约外期间 → 拒绝派发 |
| `orchestrator_v2` 确认/反思路径 | `_inject_skills` 后**再按契约重建一次**（技能/历史注入会改写指令，重建是幂等的） |
| `orchestrator_v2` 历史注入 | 模板路径的历史经验、技能里的历史教训：与契约期间冲突的表述加"本次不适用"前缀（**保留原文，不删历史库**） |
| `worker_base.SearchAgent` | `_query_variants` 查询来源优先级：派发载荷的**契约字段** > 指令 `[检索查询]` 行 > 整段指令；变体里含契约外期间的一律剔除并记日志 |
| `workers/web_fetch_worker` | PDF 的**原始字节**落任务工作区工件（`project/fetched/<sha16>.pdf` + sha256），结果带工件引用 |
| `orchestrator_v2._try_pdf_evidence` / `_artifact_bytes` | 解析**消费同一份字节**（校验 hash、路径必须落在任务工作区内）；工件缺失/校验不符 → 按缺口处理，**不再按 URL 重抓** |

## 3 离线集成反例（用实机那一版修订稿的形状）

`test_delivery_chain.TestExecutionContractThroughReviewAndDispatch`（5 例，只替身模型回包与传输边界）：

| 断言 | 结果 |
|---|---|
| 修订稿按契约重建：两条按年短查询（2023/2024 年度报告）、契约字段、查询行无 2025 | 通过 |
| 查询行含"2025年三季报" → 判违规；**指令正文**里的历史提示不判违规（标注不适用） | 通过 |
| FAIL→修订→PASS：复评看到的是**重建后**的计划，PASS 绑定这一版（`review_passed_for` 为真） | 通过 |
| 派发载荷带契约（指纹与任务契约一致）；陈旧指纹 → 重建后再发 | 通过 |
| 送到 SearchAgent 的 query：**无样板**（每个数字须能回溯/阅读重点/bank_corporate/银行对公视角）、**无 2025年三季报、无 2026**；干净短查询排第一 | 通过 |
| 契约重建失败 → 拒绝派发（队列无消息） | 通过 |

## 4 已存真实材料的整链闭合（B-5）

`test_delivery_chain.TestStoredMaterialChainClosure`（3 例）。材料为 `evals/real/`
的两份真实摘录（东财公告文本 API，art_code/披露日/页码齐备）：

| 环节 | 断言 |
|---|---|
| 取得 | 真实摘录文本装进真 PDF 容器（本仓库 `report_pdf` 写出），字节落工件并记 sha256 |
| 解析 | `fetch_bytes` 打成"一调就失败" → `doc_from_url(..., data=工件字节)` 仍解析出正文与页偏移，含真实数字 288.76 / 12.83（**证明没有重抓**）；篡改字节 → hash 不符拒绝；路径越界 → 拒绝 |
| 定位 | 切分出的记录带真实页码且准入为 admitted |
| 对应问题 | 真实披露只解释收入：不得产生现金流的经营解释；未覆盖项以 `missing_labels` 列出 |
| 无材料 | `ok=False`、`located=0`、**待查材料清单非空**（不沉默、不用检索摘要冒充原披露） |

## 5 未验项（如实）

- **公开原文获取**未在本批实测：B-5 的"再验证公开原文获取"需要一次真实抓取，
  本批按指令只做离线；抓取通道由工件路径与既有 `net_policy` 边界覆盖，
  真实站点的 200/404/反爬行为仍属未验。
- **真实 Redis / 实机派发**未验：契约字段随派发下发的实际 Redis 载荷只在替身队列上断言。
- 期间冲突的**标注不适用**只覆盖"历史经验 + 技能教训"两处注入；其它提示词来源
  （如反思产出的自由文本）仍可能带别的年份——由派发前重建与查询行不变量兜底。
