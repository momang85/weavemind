# 浏览器正常入口测试证据（2026-09-22 晚）

对象：服务装载 `c7056d2`，登录会话 `momang · 管理员`（有效）；任务 `ui-706c5ef4a5`（洋河 2023–2024 / bank_corporate）。
边界：全程无模型调用、无付费实机；全局 0/0/0、counted=False、模板/权限未动；修订走正常页面入口（`ResearchBriefPanel` 表单 → `POST /api/task/<id>/review/edit`）。

## T1 工作台结构核对（通过）

控制台 `/?conv=conv-1b12997944` → 项目结果 → 查看，完整结果视图可见：

- 面板顺序：结论速览 → 研究状态 → 关键发现 → 缺口（必需数据/证据/引用分开）→ 证据（未取得正文定位：业务背景、经营变化解释、财务附注、风险因素）→ 还不能证明什么（3 条）→ 字段位置（底稿）→ 图表（6 张）→ 修改与重验 → 验收三行 → 导出 → 溯源统计 → 可重算底稿。
- 验收三行原文：`机器验收：pass（本版 7b73a8cef8a4，绑定本版正文）` / `计划评审（Critic）：PASS（属版本 60781cdc76e7，当前版本无评审结论）` / `人工复核：待复核（无研究员批准记录）——机器重验通过不代表已复核`。旧评审不冒认到新版本，如实。
- 溯源统计：数字溯源率 83%（209/252）、金额溯源率 100%（103/103）、不可溯源 43 条逐条列出；底稿表每行带来源位置链接与 `unverified` 核实状态。
- 历史页任务内联展开渲染同一份简报：图 1–3 内联、其余 3 张入附录、研究问题与下一步四条各带支持/边界/下一步、「待核查主张（未与底稿事实绑定）」独立成块。
- 历史页「对话」标签首次打开时显示空为后端未连上的瞬时态，复验正常列出（非缺陷）。

## T2 修订 UI 正向链路（通过，产生一个真实新版本）

修订内容：读者正文里残留的 5 处 `如图 chart_N 所示（内部id）` 改为 `如图 N 所示`，3 处图题 `［core\_scale］/［yoy\_growth］/［ratio\_net\_margin］` 内部标识删除（离线预生成，8 处替换各命中 1 次；图片文件引用 `![chart_N.png](…)` 保留）。

- 页面整份提交 → 返回并显示：`新版本 5d0116b67c2f（父 7b73a8cef8a4）；验收 pass，交付已绑定该版本`。
- 验收三行随后更新为 `机器验收：pass（本版 5d0116b67c2f，绑定本版正文）`；Critic 行仍属旧版本、不冒认。
- 导出块同步：`包生成于 dc60226e6ef6，当前版本 5d0116b67c2f——包不含最新修订，请重新导出`。
- 离线四方核对一致：版本库采纳正文 sha256 `5d0116b67c2f9047`（15,721 字符，`如图 chart_` 已消）；`acceptance_report.json` pass @ 同 sha；`research_state` 绑定正文同 sha；导出清单 body_sha256 同 sha。
- 负例先行：find 填不存在片段 → 页面显示 `find 未出现在当前版本正文中`，不建版本（机器验收行不变）。

## T3 安全负例（通过）

从页面会话发起：

- 未带凭据（`credentials:'omit'`）：`POST /api/task/<id>/review/edit` → 401；`GET /files/<id>/charts/chart_1.png` → 401；`GET /task/<id>` → 401。
- 编码路径穿越：带会话 `GET /files/<id>/%2e%2e%2f%2e%2e%2fconfig.json` → 404。
- 文件白名单（带会话）：`charts/chart_1.png` 200（51,137B）、`export_manifest.json` 200；`project/chart_1.png`、`project/working_paper.json`、`budget_state.json`、`llm_calls.jsonl`、`reports/model_report_full.md` 均 404（内部件不外露，底稿走专用 API）。
- 附带观察：HEAD 请求一律 501（服务未实现 HEAD，探测需用 GET；非缺陷）。

## T4 导出核对（通过）

- `GET /api/task/<id>/report.md`：36,906B，sha `14f9d71d…`；响应头 `x-report-version-id=59f12824…`、`x-report-research-body-sha256=5d0116b6…`、`x-report-draft=0`；正文已无 `如图 chart_`。
- `GET /api/task/<id>/pdf`：5,582,481B，`%PDF-1.4`，sha `5abe9e09c3efbc9a…`；响应头同样绑 5d0116b6。
- 导出后落盘清单已刷新：`files.markdown.sha256=14f9d71d…`、`files.pdf.sha256=5abe9e09…` 与下载字节一致；`status=verified / aligned=true`；`research_state`（stale=true）随清单携带。
- 旧 ZIP `deliverables_20260922_120247.zip`（生成于 12:02，属 dc60226e）在导出块如实标陈旧；**同版新 ZIP 仍未打（遗留项）**。

## T5 研究状态绑定显示（如实记录：仍待重验）

- 页面与 PDF 首页均显示：`研究状态：研究草稿／待补原始披露`，`研究状态与采纳正文不是同一版（结构 fd112fa7d23c ≠ 正文 5d0116b67c2f）：待重验，计数不作为结论`。
- 核对落盘：修订后的装配**已重建结构投影**（`report_structure.json` version 4dd52a68→fd112fa7d23c），但 `research_state.binding` 的 `evidence_fingerprint`/`rules_version`/`contract` 为空 → 按"绑定信息缺失标待重验"规则仍 stale。**读端行为正确；写端在修订路径未补证据/规则/契约指纹是剩余缺口。**

## T6 新 PDF 逐页核对（下载落盘 + 逐页目视）

浏览器"下载PDF"按钮 → 系统下载 `ui-706c5ef4a5.pdf`，sha256 `5abe9e09c3efbc9a…`（5,582,481B）与清单一字不差。bundled 运行时 `pypdfium2` 渲染 15 页，逐页目视（visual-judge 子代理两次因账号连接不可用无法启动，按回退由执行者逐页检查）：

- 正向：无豆腐块；负号/公式减号全部可读（如 `(288.76 - 331.26) / 331.26 * 100`）；数字与"亿元"未被拆行；6 张图全部真实嵌入且图内文字可读；图题已是人读形式（`图 1：洋河股份 2023 与 2024 三个核心指标的规模对比如何？`）；表格列对齐；阅读顺序 简报（1–4）→ 分析（5–10）→ 汇总复核/变化解释/风险核查（10–12）→ 附录（12–15）。
- 发现问题（按页）：
  1. **p6 页底孤立标题**「3.3 含义解读」，正文从 p7 开始（标题随正文分页未兜住这一条）。
  2. **p8 §6.1 负号与数字断行**：`大于营业收入降幅（-` / `12.83%）` 跨行，负号留在行尾（`_safe_break` 未保护"负号+数字"整体）。
  3. **p9 §6.3 引用不存在的表**：正文写"（见 6.3 表）"，但 6.3 节下没有任何表（模型正文既有内容问题，非本次修订引入）。
  4. **p10「关键数据一览（汇总复核）」空节**：标题下无内容直接进入「变化解释」（同上，既有内容问题）。
  5. p12 待核查主张列表残留字面 `**`（模型正文未配对加粗标记，如 `…体现**`、`差异来源**：…`）。
  6. p5 两处句号被换行甩到行首（`…获取时间）。` / `…相关小节。`）。

## 本轮未验/遗留（如实）

1. 同版 ZIP 未打：旧包 dc60226e 与当前版本 5d0116b6 不一致，页面已如实提示"请重新导出"。
2. 研究状态写端缺口：修订装配重建了结构投影但未补 evidence/rules/contract 指纹，状态继续待重验（读端正确）。
3. 主文收敛（2–4 页目标）未做；PDF 首页仍以完整任务指令为标题块（属该遗留项的一部分）。
4. T6 第 3–6 项为模型正文既有内容问题，需在后续候选修订中处理，不在本轮改动。
5. 面板截图因页面过大（约 95KB DOM）surface 准备超时未取；本记录以 DOM 快照文本与落盘 hash 为据。
