# 登录态实机检查与"最后一公里"修复（2026-09-17）

本轮做的是此前唯一的空白项：**在已登录的真实服务上跑真任务并逐项对照检查**。
结论按"已修 / 已发现未修 / 环境受限"三类分开，不合并成一句"通过"。

- 环境：`http://localhost:8080`（服务进程 `web_ui.py` 启动于 21:08，**仍是改动前的代码**；
  渲染类修复靠 `frontend/dist` 重建即时生效，Python 侧修复要等下次重启，见文末"待生效"）。
- 模型：LLM 端点 `tokenrhythm.studio`，planner 角色指 `qwen3.8-max`；
  运行期实测 **planner 连续两次调用失败**，且 Embedding 端点**额度耗尽**（`/api/events` 可查）。

## 一、真任务跑了什么、得到什么

| 任务 | 目标 | 终态 | 验收 | 交付状态 |
|---|---|---|---|---|
| `ui-24a59d1c7b` | 贵州茅台 2023/2024 营业收入·归母净利润·经营现金流（合并口径） | `SUCCESS_WITH_ISSUES` | `fail`，4 条缺口 | 未验收草稿 |
| `ui-01efce721a` | 三句话解释营业收入与经营现金流的区别 | `SUCCESS_WITH_ISSUES` | `fail`，3 条缺口 | 未验收草稿 |
| `ui-72db46f6fd` | 渠道改革检索（用于取消检查） | `CANCELLED` | — | — |

第一条任务的失败链路是**可复核的**，不是"看起来失败"：

- 规划两次失败 → 回退单步 `content_summary`（日志 `Plan failed: LLM call failed after 2 attempts`
  → `Plan fallback: single content_summary step`）；
- 计划里没有检索步骤 → 报告**没有任何来源**，验收给出
  `数字溯源率 0%（0/63）；引用 0 / 计算 0 / 模型知识 23；财务金额溯源率 0%（0/15）`、
  `目标要求引用官方/可核实来源，报告未给出任何来源链接`；
- 交付物本体上写明"未验收草稿"与缺口，而不是把无来源的报告当成功交付。

**这是环境受限的结果**（planner 模型不可用 + Embedding 额度耗尽），不能当作"检索/研究链路
稳定"或"报告质量"的证据；它证明的是**缺口会被如实暴露**。

## 二、本轮发现并已修（4 项，都有回归）

1. **交付状态恒为"未知"，最后一公里丢结论**
   验收绑的是报告步骤写出的中间正文（4565 字节 `fail`+4 缺口），收尾把交付说明/评审注记/
   底稿缺口拼成 12184 字节的正文并采纳——两者字节不同，于是交付只能显示
   "该版本没有对应它自身的验收（未知）"。修法：收尾对**被采纳的那份正文**补一次确定性验收
   （幂等；自动修复改写正文时不硬绑，如实留在未知）。
   在真实任务副本上验证：`overall` 由 `''` → `fail`、`本版验收?` 由 `False` → `True`。
2. **`elapsed_sec` 多算 8 小时**：SQLite 的朴素 UTC 串被 `.timestamp()` 按本地时区解释，
   实测"已运行 2 分钟"的任务返回 `28854`。已抽出按 UTC 解析的时间函数；同批把
   "没有 timestamp 的日志推送"落成 UTC ISO（此前落服务器本地裸钟，同一列日志一半本地
   一半 UTC，页面上出现 `59+00:00`、`:14.834Z` 这类残片）。
3. **人工改版不进交付正文**：`review/edit` 只写版本库，页面/导出/PDF 读的是任务 `report`
   字段——接口返回 `ok`，导出的 Markdown 字节与改版前**完全一样**。已改为同步交付正文
   （映射不上就不动，并如实返回 `delivery_updated=false`）；状态与理由统一走
   `verified_delivery`，不再抄"最后一次交付记录"（改版后结论与理由自相矛盾）。
4. **窄屏输入框被压成 24px**：390px 窗口下，输入行的按钮组 `shrink-0` 占 319px，主输入框
   只剩 24px（实际不可输入）。已让按钮组在窄屏独占一行；复测 390px 输入框 345px、
   无横向溢出，1280px 布局不变。

## 三、本轮发现、**未修**（如实挂账）

1. **`identity_id()` 会漂移**：`bind_acceptance` 会把 `rules_fingerprint` 覆盖成验收里的值，
   而身份 = `sha256(version_id|sources|rules)` 且被当作版本库的键。于是同一版本的
   "重算身份"与"入库时的键"不再相等（实测：入库键 `7faeb4d9…`、重算 `2033fae7…`，
   同一份 `report_versions.json` 里两者都在）。页面/清单里那个 `report_version_id` 是重算值，
   与库里的键对不上——身份不再能作为跨时间的稳定标识。修法涉及 R0.2 的身份契约
   （身份排除规则指纹，或绑定后必须重算并重键），需架构确认，未擅自改。
2. **运行中刷新页面不再跟踪任务**：运行中重载后，后端仍有该任务的日志，但页面
   日志/进度不再出现（`taskId` 只在内存 store 里）。刷新恢复跟踪属 C 批的范围，
   本轮只记录现象与证据。
3. **文档级数字-主体绑定**缺口（既有挂账，未变）。

## 四、本轮检查过的其它项（通过）

- **取消**（`ui-72db46f6fd`）：`POST /api/task/<id>/cancel` → 200；终态 `CANCELLED`，
  日志给出两条**分开的**时限："自请求停止起 0.6s 生效，在时限内"、
  "在飞调用的供应商结算窗口 900s（可能仍在计费，按待对账记账）"——与 R2 的口径一致。
- **导出绑定**：`/api/task/<id>/report.md` 与 `/pdf` 都带 `X-Report-Draft: 1` 与
  同一 `X-Report-Version-Id`；PDF 5.3MB、magic `%PDF-`。
- **复核改版接口**：未命中片段 400（不改任何东西）、同正文 400、正常改版 200 且
  `needs_reverify=true`、新版本 id 与 parent 不同。
- **底稿接口**：该任务无结构化事实 → `GET /api/task/<id>/working_paper` 404
  （不编一份空底稿）。
- **日志时钟列**：重建 `dist` 后逐行读 DOM，时间戳渲染为本地时间（`21:56:48` 等）。

## 五、待生效 / 待你参与

- **Python 侧修复要等重启**：当前服务是 21:08 启动的旧进程，`elapsed_sec`/日志时间戳/
  交付状态理由这三项在下次重启后才在页面上生效（代码与定向测试均已验证）。
- **真实模型质量仍未验证**：本轮规划/反思因端点不可用而降级，报告无来源是**环境受限**，
  不能据此判断研究链路质量；planner 模型名与 Embedding 额度需要你确认真实可用后重跑。
- 银行投研/对公试点、港股/美股真实覆盖：按指令后置，门槛不降。

## 六、重启后的实机复核（同日 23:26 重启，全部为页面上实测）

服务用 `python launcher.py stop && python launcher.py start` 重启（16/16 服务存活），
随后在同一个已登录页面上逐项复核：

| 复核项 | 实测结果 |
|---|---|
| `elapsed_sec`（运行中，此前 28854 的场景） | 新任务运行中依次 11.5 → 33.9 → 56.2 → 78.5 → 100.9 秒；收尾 222 秒 ✓ |
| 日志时钟列 | 逐行读 DOM 为本地时间（`23:27:56` 起），无 `+00:00` / `:xx.xxxZ` 残片 ✓ |
| 交付状态理由（页面） | 旧任务 `ui-01efce721a` 由"该版本没有对应它自身的验收（未知）"变为 **"验收未通过（overall=fail）"** ✓ |
| 新任务的交付绑定 | 新任务 `ui-46f09227bf`：选中版本 `65c8118e…`，**验收 `report_sha256` = 该版正文 hash**，`overall=fail`、2 条缺口；页面显示同一结论 ✓ |
| 人工改版进交付 | 对该任务做 `review/edit`（改"## 免责声明"一节）：`delivery_updated=true`、导出的 Markdown **含修订**（+12 字节）、`needs_reverify=true`、重验后页面指向新版本 `a1be6687…` 且理由仍为"验收未通过" ✓ |

两点如实说明：

- 新任务的验收事件只有 `内容摘要步骤` 一条、**没有** `最终装配`——说明这次"被采纳的正文"
  与"报告步骤写出的正文"是同一份，收尾那一步按设计**跳过**（幂等）。`最终装配` 只在两者
  字节不同时触发，其机制由单测（`test_r0_boundaries.TestFinalBodyAcceptanceBinding` 4 项）
  与真实副本脚本验证。
- `identity_id()` 漂移（第三节第 1 条）**仍未修**：页面/清单里的 `report_version_id` 是重算值，
  与版本库里的键不是同一个串。

