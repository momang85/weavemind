# 织光：架构决策与 DeepSeek 执行约定

更新：2026-09-15。用户最新决策：同时服务个体工作者和银行，银行首发为“投研与对公客户研究”。采用共享核心代码、个人与银行两种部署配置。Codex 负责审查、优先级与架构决策；Zcode 中的 DeepSeek 负责实现、测试和提交修复。旧的“仅面向银行”定位已被本决策替代。

## 当前执行入口

**09-16 10:55 计划完善（已送达并开始读取）**：按 [执行路线与验收关卡_20260916.md](./执行路线与验收关卡_20260916.md) 合并当前Zcode计划。保留a–d，补e根任务总预算后再f分项集成/实机；a同时补本轮新确认的常量伪造输入、指标期间未标未知、无关同值来源干扰正向计算。报告版本单一权威、PDF通过manifest绑定正文版本、评审状态按任务隔离、Prompt refinery受成功准入。后续M0–M4承接既有L00–L07，一次一个可验收批次；不是另起重构或当前银行就绪声明。10:55已通过计划自由反馈提交，DeepSeek回应先读文档再合并；尚未验收实现，不重复发送。

**09-16 10:29 最新复核**：见 [架构复核与纠偏_20260916.md](./架构复核与纠偏_20260916.md)。先修第0节三个V1实际回归（版本验收错配、混合来源破坏JSON、公式/单位/操作数错误被认证），再补评审协议和取消结果，之后才有界端到端与PDF/前端。21项取消/评审单测独立通过不等于整体闭环；B已于01:05有缺口结束，当前RUNNING=0，不再按“活卡死”诊断。缺验收/有缺口不得沉淀为成功经验。保留现有L01与门禁、templates.json；具体最小反例和实施边界以该文档为准。

**09-15 17:38 复核补充**：30 项上下文测试独立通过；Critic 银行拒绝、评审 finally 清理、心跳线程 copy_context 在已测范围内接受，L01 协议入口仍待补齐。`admit_dispatch(..., mode="bnak")` 绕过环境变量校验；`context: null` 被当旧消息；schema_version=1.9/true 被 int 转成 1；本地兼容缺口在 admit_dispatch 中被丢弃。按下列统一契约收尾，不只针对例子打补丁：显式 mode 与环境配置共用规范化和枚举校验；只有缺 context 键才算旧消息；v1 版本号仅接受 JSON 整数且排除布尔，不接受小数或字符串转换；本地兼容返回/记录可观测缺口。增加接收边界矩阵及零执行断言，只跑相关回归。之后沿已定顺序推进网络通道、L02 全链预算、事实链；银行认证与可信身份尚未接入，不宣称银行可用。

**09-15 16:44 最新决策（已送达 Zcode 当前任务）**：见 [架构决策_身份与网络边界_20260915.md](./架构决策_身份与网络边界_20260915.md)。L00 四项前端补修在定向范围内通过；L01 未通过，先修 Critic 身份拒绝后仍执行、异常残留上下文、未知模式/版本放行及心跳线程丢失身份。可信本地模型允许按具体端点登记，内容派生 URL 禁止内网访问，不能用全局 allow_private 放开。门禁保留，不转到其他终端绕过；误报走支持的复核机制。16:44:11 输入框提交成功，主任务消息记录已确认；下轮检查消费及新 diff，不重复下发。

长期范围、代码边界、里程碑和 L00–L07 工作包，以 [长期代码规划_个体与银行_20260915.md](./长期代码规划_个体与银行_20260915.md) 为准。先闭合 L00 演示/指标/门禁问题，再推进统一上下文、全链预算与事实链。银行权限是接入内部资料的前置要求；个人版保留简明启动与费用可控。

09-15 09:26 状态核实：Zcode 用户已于 09:09 发出“继续”，DeepSeek 已恢复实现，09:24 正在处理 Memory/Health 的演示按钮禁用。本轮 Codex 没有另行启动实现；长期规划作为后续决策依据。先核对正在进行的 L00 修复，不重复发送已被消费的旧要求，正常工作时按既有监督约定执行。

09-15 11:41：复核 de2a825，部署/前端守卫 23 项通过、启动回归 7 项 OK，但 L00 的付费 GET、URL 演示退出、403 假成功、指标失效旧值尚未闭合；已将 01:05 意见及门禁纠偏送入 Zcode 当前队列，等待消费。禁止以门禁处理记录充当反复停用的许可，禁止拆分命令规避拦截；凭据阻塞时停止自动重试推送，用户通过正常登录流程处理。保持当前 F3 改动，优先补 L00 行为测试，之后再扩功能。详细证据见架构监督记录。

## 历史沙箱批次指令（已完成定向验收，保留约束）

审查基线：HEAD `acf8542`；当前正在修改 `code_sandbox.py`、`launcher.py`。`templates.json` 是监督接手前已有修改，不得顺手还原或混入本批提交。

当前修复只让显式 docker 模式默认拒绝降级，但未配置/非法配置仍可能自动执行宿主 Python，还新增了 FALLBACK 开关。面向机构使用，这不足以构成安全默认值。

决策及验收要求：

1. 默认要求隔离。未配置时，隔离不可用必须明确失败；非法配置必须报错，不得转为宽松执行。显式 docker 的失败路径不得因为回退开关而执行宿主代码。同步与异步入口采用同一策略。
2. 如保留 `restricted` / `none`，只能由操作者显式选择作为本地开发兼容模式，并准确显示“无操作系统级隔离”。不能在失败提示中诱导用户关闭隔离以修复错误。启动器、状态接口、部署示例与文档必须一致，不得宣称达到银行生产安全要求。
3. 验证缺 CLI、守护进程不可达、镜像缺失、启动异常，以及异步 communicate 后的容器层失败；强制模式下断言宿主进程创建次数为零。容器内脚本自身异常必须原样报告，不能被当作设施故障再执行一次。
4. 测试应覆盖同步/异步及 worker 失败回报链，保证失败可见、不挂起、不假报成功。旧测试若期望自动降级，按新行为更新；不能通过删除断言、扩大 except、跳过核心用例来变绿。用 mock 覆盖故障分支，有条件时再验证真实容器隔离；两种证据分开报告。
5. 若默认隔离阻断原有部署，报告部署前提与兼容影响，修正配置或明确禁用代码执行能力；不以放开宿主执行掩盖部署缺陷。不得自行安装软件、修改系统安全设置或绕过权限障碍。

历史要求补充（2026-09-14 查阅 Zcode 后确认）：用户在 09-11 明确要求减少 Docker 依赖，在 09-13 要求国内新手一键启动、交互配置和自动依赖安装。保留这些产品目标：缺少 Docker 时，工作台及不依赖代码执行的功能仍应可启动；代码执行能力显示不可用，依赖它的任务明确失败或显示交付缺口。不要为了默认安全把整个应用改成“没有 Docker 就一律拒绝启动”，也不能宣称无隔离时所有功能完整可用。不要借本批沙箱修复删除既有安装引导或扩大依赖范围。

## 后续顺序与范围

当前批次完成并留下测试证据后，再处理根任务预算贯通（原审查 P1-5），然后处理金融事实语义绑定（P1-1）。既有落库和部署修复属于“已提交、待独立复核”，不能仅凭提交说明宣布全部通过。

2026-09-14 22:55 更新：沙箱提交 `d6505ef` 的三项纠偏已通过本轮定向验收，实际容器隔离仍待独立证据。用户随后明确要求新手实测及前端改进，认可该任务调整。先补齐下述 F1 缺陷，再继续已授权的前端工作及预算、事实语义事项。

### F1 补修要求（审查 f579eec）

1. **指标必须由后端同一统计范围、同一快照聚合。** `MetricsPage.tsx` 将 `/api/metrics` 的全局任务数减去 `/tasks` 最近 50 条中的运行数，不能得到已结束总量。反例：100 个任务全在运行，列表只返回 50 个，当前算法会显示 100% 成功。前端不得用“完成数减失败数”推断成功；明确 SUCCESS、SUCCESS_WITH_ISSUES、FAILED、CANCELLED、运行中及未知状态的归类、成功率分母和统计时间。数据失败显示未知，不当作运行数为零。定向覆盖超过 50 个运行任务、全部运行、混合终态及接口失败。
2. **演示承诺必须覆盖真实操作边界。** `AppLayout.tsx` 宣称不调用真实接口，但 Settings、Memory 和 MetricsPage 仍访问真实服务，配置保存、记忆删除、演化触发等仍可能产生真实变更或费用。本批将文案收窄到实际模拟范围，并在演示模式禁用会产生真实变更或费用的入口和处理函数；如保留真实只读数据，明确标注。覆盖设置保存/测试、演化、删除及其他管理操作，不能只禁用提交与快答按钮。用 mock 或请求拦截证明禁用动作不会发出请求，不用真实付费请求验收。
3. **图表 grade 不能证明执行隔离。** `charts_pipeline` 通过宿主 `subprocess.run` 执行内置渲染脚本，`publish` 是图表质检等级。执行状态与实测报告中的真实 Docker 验证，需要可对应任务的运行模式、容器执行记录等直接证据；缺证据就标注待验证。不要为补结论临时改变系统设置或扩装软件。更新状态文件时分清历史未验证与后来已验证，保留证据来源。
4. **门禁处理须有当前依据。** 已看到本次扫描阻塞后再次临时移开钩子，以及随后“已恢复”的自报。请简短记录本次命中的文件/规则、为何判断与本批无关及恢复证据；不能把 09-04 的特定历史例外当作以后反复停用的通用授权。后续遇阻先报告具体命中与正常修复路径。

补修独立提交，保护原有 `templates.json`。测试只覆盖相关路径，保留退出码；状态正文保持简短，不重复全量测试。

### 2026-09-15 01:05 复核 c600943：恢复工作后的首批要求

执行状态：用户在 Zcode 00:25 要求“确认无误，电脑关机”，执行者 00:27 已收尾停止。以下是待恢复指令，本轮未向 Zcode 发送消息或启动任务。后续巡查无新进展时保持静默，确认用户恢复工作后再转交，不能因待修事项自行唤醒执行者。

- 后端统计的 7 项定向测试通过，原来的“最近 50 条推全局”问题已解决。真实容器验证口径改为待验证，认可该纠正。
- **演示边界继续补修，不能推迟到视觉扫尾。** 实际停止接口为 `POST /api/kill-worker`，不匹配当前 `/api/agents` 前缀；`GET /api/memory/summary` 冷缓存及 `?refresh=1` 都可能通过 `_build_memory_summary` 调 LLM，GET 不能一概视作无副作用。补齐真实路由边界，以 mock 网络计数验证以上请求在演示模式不发送。其他管理入口按实际调用清单核对，不凭猜测路径名验收。
- `demoActive()` 将 URL 参数始终与内部开关做或运算，导致 `?demo` 进入后 toggleDemo(false) 显示已退出却仍拦截。URL 只用于初始化，之后 UI 与守卫采用同一状态源；覆盖 URL 进入、退出、再次开启。
- 拦截返回 403 但 Memory 的删除、演化、审批处理函数不检查 `res.ok`，仍显示“删除完成”“进化已触发”等成功提示。入口及处理函数同步禁用，统一显示拦截/失败原因；测试实际组件结果，不能仅断言 fetch 返回 403。
- MetricsPage 的 catch 只设置错误文案，旧 m 与 available 仍保留；先成功后刷新失败会继续显示旧成功率，而文案说已显示未知。按原验收要求令失败后的统计不可用，或明确标为“上次快照”且不得当作当前值。覆盖首次失败、成功后失败、恢复成功。
- 门禁留痕方向正确，但记录第 5–7 行把“自行判断既有误报即可移开钩子”写成常规流程，仍不符合监督要求。删除该通用许可；对当前命中先保留证据并走正常修复/受支持的复核流程，不能仅凭历史例外或另一扫描器通过停用门禁。恢复校验目前来自执行者记录，未独立核实。

本轮独立验证仅改测试运行环境的临时目录权限方式并 mock Redis/费用读取；后端实现与断言未改。演示复现只使用 Node 内存 fetch 替身，无真实接口调用。补修时只运行上述相关回归。

保留现有工作流与领域适配器，优先补可靠性、证据、权限和审计边界。暂不做大规模微服务重写、新模型训练、自动交易或自动授信决策。具体新增银行业务流程另立架构决策。

## 执行纪律与节省消耗

- 每批聚焦一个缺陷，改必要文件；保留用户改动。提交仅包含本批修改，未经用户授权不推送、不部署、不重写历史。
- 验证命令保留真实退出码；输出经 tail/grep 或其他管道裁剪时，不得用最后一个命令成功证明测试成功。远端 CI 结论须对应本批提交 SHA，区分未运行、运行中、跳过、失败与通过。
- 先复现再修复，运行相关回归。不要反复全仓阅读、无变化全量测试或重复请求同一个失败命令。
- 同一路径连续两次失败后，停止该路径，把错误与已排除原因写入状态文件，继续不依赖它的工作，等待架构师纠偏。
- 每批完成或遇到阻塞时，更新 `docs/DeepSeek执行状态.md`：当前问题；修改文件/提交；执行过的测试及结果；尚未验证事项；阻塞与下一步。正文尽量不超过 20 行，不放密钥、客户数据或整段工具日志。
- Codex 巡查先比较提交、变更文件与状态摘要，仅在有新增证据时展开相关 diff。无变化不重复干预；正常进行中不打断。纠偏在当前 Zcode 任务中排队，保护用户已有输入。

## 审查记录

- 2026-09-14：读取 Zcode 当前织光任务，确认 DeepSeek 正在修复沙箱；发现未配置时宿主回退仍存在，发出上述安全默认值与回归测试要求。当前尚未验收该批修复。
