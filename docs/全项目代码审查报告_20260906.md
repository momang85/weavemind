# 全项目代码审查与功能运行状态报告（2026-09-06 晚）

**范围**：后端全部模块（编排/worker/适配器/验收/记忆/演化/通知/定时/指标/守护）+ 前端 8 页面 + 全部 API 路由 + 配置开关。
**方法**：能力注册 vs 任务实际派发对比、API 路由 vs 前端消费对比、配置开关 vs 实际生效、浏览器逐页核验、关键功能实况验证（端口/日志/HTTP）。
**结论**：整体健康；发现并修复 2 个真实缺陷；确认 6 处“功能未发挥应有效果”。

---

## 一、本轮发现并修复的真实缺陷

| # | 缺陷 | 证据 | 修复 | 验证 |
|---|------|------|------|------|
| 1 | **cron 定时任务每次服务重启立即触发**（错过时刻也补发） | 17:46 与 18:10 两次启动日志都出现 "Scheduled job fired: 每日 A 股活跃度日报" | `_is_due`：当日时刻已过且从未触发 → 返回 False（次日按时触发） | 单测：18:10 启动不触发、次日 16:00 触发、同日不重复；线上重启后 runner 只打 started 不再 fire |
| 2 | **metrics 成功率恒 0%**（9 任务全计失败） | metrics_summary.json: failed_tasks=9, success_rate=0.0；实际任务大量 SUCCESS | ①task_complete 进度消息参与统计；②SUCCESS_WITH_ISSUES 计成功；③CSV 逐行 flush（此前 metrics.csv 长期 0 字节） | 单测 3 任务分类正确（2 成功/1 失败）、flush 3 次 |

## 二、全功能运行状态矩阵

### 🟢 正常发挥应有效果（核心链路全部实测通过）

| 功能 | 证据 |
|------|------|
| 任务编排（规划→并行 DAG 派发→反思→验收） | 十余次任务全程实测，能力派发统计正常 |
| 10 类 worker 能力 | web_search 536 次 / content_summary 495 / code_execution 581 / report_generator 388 / package 435 / data_analyzer 103 / data_loader 32 / model_trainer 21 / react_agent 13 / web_fetch 215——全部被真实调用 |
| 结构化数据预载（财务/排行/宏观/crypto） | sina_ranking 5555 行、financials 港股 2 年报等多次实证 |
| 图表管线（确定性语义图+LLM 规格+QA+去重+分级） | 本轮 D4 限频上线后日报任务仍正常出图 |
| 验收器（三档溯源/来源声明/死链/交付完整性） | 任务日志 Acceptance 逐次运行，日报落地页溯源率 85% |
| 反思自修复闭环 | 溯源率 6%→23%→55%→85% 历次爬升 |
| 报告生成与渲染（ReportViewer/分享页/PDF） | 逐帧监测+落地页实测 |
| 会话鉴权（登录/防爆破/角色） | 登录/未登录 401 行为实测 |
| 会话持久化（C1） | 重启后 cookie 存活实测 |
| 历史/对话/项目多工作区 | 页面与 API 实测 |
| 评测集自动生长 | auto_grown 多次追加（正常特性） |
| 评测闸门（CI） | CI 持续 success |
| 定时任务 runner | 修复后正常运行，16:00 内置日报 |
| 通知通道（webhook/Server酱/邮件） | 代码与配置就绪，日报终态已接线 |
| 分享链接（密码/TTL） | 日报自动分享实测 |
| 溯源体检 API（/api/verify） | 实测返回三档分类 |
| 前端 8 页面 | 逐页核验全部渲染（含数据加载） |
| Critic 评审 | worker 订阅 plan_draft，872 条评审日志，config critic=true 生效 |
| Guardian 进程守护 | 心跳/隔离期日志正常 |
| 演化沙箱 | evolution_result 发布路径存在，评测页面就绪 |

### 🟡 可用但降级/未启用

| 功能 | 状态与说明 |
|------|-----------|
| code_sandbox docker 模式 | docker 可用但 `weavemind-code-sandbox:latest` 镜像不存在 → 每次执行降级 restricted（`docker build -f Dockerfile.sandbox -t weavemind-code-sandbox:latest .` 可恢复完整隔离） |
| LoRA 本地推理 | 配置与管线齐备（lora_servers.json/8765），但**本机未运行 LoRA 服务**且 llm_mode=cloud → 全部走商业 API，本地推理功能闲置 |
| 通知分发 | 三通道代码就绪，默认 disabled（需用户在设置填 URL 启用）——功能可用未启用 |
| 单步确认 HITL（step_confirm） | 后端有 /api/step/confirm，前端无入口（仅计划确认有 UI） |
| 验收建议（/api/acceptance/suggestions） | 后端有、前端未消费 |
| 指标延迟统计 | task_result 延迟链路依赖的 pubsub 通道与 orchestrator 实际发布通道不一致 → 延迟恒 0（统计计数已修，延迟链路待接） |

### 🔴 代码存在但未发挥效果（无前端消费的后端能力）

| 端点/功能 | 说明 |
|-----------|------|
| /api/notifications（通知配置 UI） | 设置页无通知配置区，只能手工改 config.json |
| /api/scheduled-jobs（定时任务管理 UI） | 内置日报只能后端管理，用户无法在界面增删改 |
| /api/single-agent（单智能体直发） | 无 UI 入口 |
| /api/audit + /api/tool-audit（审计日志） | 后端记录完整审计但无查看页面 |
| /api/metrics（指标看板） | metrics_collector 收集但无展示页（数据仅落 CSV/JSON） |
| /api/projects（项目列表） | 前端项目选择器未接此端点 |

## 三、回归与状态

- test_common ✅ / test_p0(266+) ✅ / delivery(176) ✅ / TS+构建 ✅
- 线上：修复后重启验证 cron 不再误触发；服务运行中
- 改动待提交：scheduled_jobs.py、metrics_collector.py + 本报告

## 四、建议的下一步（按价值排序）

1. 给定时任务/通知/审计/指标补前端页面（四个后端能力缺 UI，工作量小、产品完整度高）
2. 构建沙箱镜像（一条命令恢复代码执行隔离）与决定 LoRA 部署（hybrid 模式价值）
3. metrics 延迟链路对齐发布通道
