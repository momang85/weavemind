# 研究员评分表（F3 验收）· 2026-09-20

**这张表是给研究员填的，不由模型自评。** 模型只提供客观读数与打开方式；五项各 0/1/2，
建议 ≥8/10，且不得存在错主体 / 错期 / 错引用 / 虚构因果这类严重问题。请记录复核耗时、
你做的关键改动、以及是否愿意再做第二份报告。

## 评分项与判据

| 项 | 0 分 | 1 分 | 2 分 |
|---|---|---|---|
| ① 问题回答程度 | 只有零散数字，读不出"公司怎么样" | 有变化与方向，但缺关键一问（如增长来源/现金质量） | 两三个最值得关注的变化清楚，缺口与下一步要查的材料写明 |
| ② 数字可复算 | 数字对不上来源或算不出来 | 主要数字能对上，个别需追问 | 每个读数都能按给出口径/算式复算（含同比、比率） |
| ③ 解释有证据 | 原因像是编的或与材料无关 | 部分解释有出处，个别越界 | 每条解释都指向具体来源位置（小节/页码），没有证据的写成待核查 |
| ④ 风险具体 | 只有"存在风险"这类空话 | 有风险条目但缺证据或触发条件 | 风险带对应证据、会使判断改变的观察条件、要补的材料 |
| ⑤ 修改复用便利 | 想改只能重跑任务 | 能改但版本/导出对不上 | 页面上直接改正文→重验→同版导出，耗时短且版本清楚 |

## 三份既有产出（试用台账 `trial_tasks.json`）

打开方式：浏览器进 `http://127.0.0.1:8080/` → 控制台"项目结果"里点对应任务的**查看**；
导出用结果页底部"导出与复核"（Markdown / PDF / 交付包）。

| 场景 | 任务 | 终态 | 耗时 | 验收 | 数字溯源 | 证据定位 | 缺口类别 | 采用来源 | 图 |
|---|---|---|---|---|---|---|---|---|---|
| A 个人投研（茅台 2023/24） | `ui-ae5b57eea5` | SUCCESS_WITH_ISSUES | 42m | pass | 91%（74/81） | 1 | 3 | 8 | 3 |
| B 银行对公（洋河 2023/24） | `ui-9a93b1948c` | SUCCESS_WITH_ISSUES | 30m | pass | 98%（80/82） | 2 | 3 | 9 | 3 |
| C 边界（招商银行 2023/24） | `ui-6dc571e5b4` | SUCCESS_WITH_ISSUES | 25m | pass | 89%（62/70） | 0 | 4 | 8 | 3 |

**这三份产出跑在 F3 之前的代码上**（19:30 进程）：图是旧的三张（比率混轴、覆盖类别退化成
"2024年"），来源清单里混着检索噪声，招行那份仍按企业口径出比率。评这三份只用于回答
"内容方向对不对"，**不能代表 F3-B 之后的样子**——F3-B 的分面图与结果页顺序需要新跑一份
才能看到（见下"待跑"）。

## 修改复用（第 ⑤ 项）· 已实测一次真实修订

在任务 `ui-f00775cfdf`（洋河对公，F3-A 代码）上走了一次**真实人工修订**（浏览器内，
与结果页"修改与重验"同一接口）：

- 操作：把正文里的 `## 分析` 改为 `## 分析（人工复核：口径与缺口已核对）`（查找/替换）
- 读数：新版本 `cc6f2190…` ← 父版本 `65ec49d1…`；重验 `acceptance=pass`；交付 `verified`；
  `needs_reverify=false`；交付说明（wrapper）逐字节复用 `stored`
- 导出：`/api/task/ui-f00775cfdf/report.md` → 200、**正文含本次修订标记**、响应头
  `X-Report-Version-Id` 与页面当前版本一致；交付包 `/files/…/deliverables_20260920_104729.zip`
  → **200（449,041 字节，可下载）**
- 耗时：**约 1 分钟**（含重验与导出），无需重跑任务

请第 ⑤ 项按这次实测评分，并记录你自己改一次的实际耗时。

## 待跑（需要先解决端点上限）

想按 F3-B 之后的样子评分，需要新跑一份实机（分面图 + 新结果页顺序）。当前**跑不了**：

```
POST https://ark.cn-beijing.volces.com/api/v3/chat/completions
→ 429 SetLimitExceeded：账号 [2132184326] 的 glm-5-3-flash 达到推理上限，
  模型服务已暂停；需在"模型激活"页调整或关闭"安全体验模式"
```

按约定实机要预声明上限（单次 ≤25 分钟、≤40 次调用、费用口径不可读则如实报"不可控"）。
端点恢复后建议跑：**洋河股份 / 002304.SZ / A股 / 2023–2024 / 合并 / 银行对公 / 截至 2025-04-30**
（与场景 B 同契约，便于与上表逐项对照），跑完把下面空表补齐。

## 评分记录（研究员填写）

| 项 | A 投研 | B 对公 | C 边界 | 新跑一份（待补） | 备注 |
|---|---|---|---|---|---|
| ① 问题回答程度 |  |  |  |  |  |
| ② 数字可复算 |  |  |  |  |  |
| ③ 解释有证据 |  |  |  |  |  |
| ④ 风险具体 |  |  |  |  |  |
| ⑤ 修改复用便利 |  |  |  |  |  |
| 合计（/10） |  |  |  |  |  |
| 严重问题（错主体/错期/错引/虚构因果） |  |  |  |  |  |
| 复核耗时 |  |  |  |  |  |
| 关键改动 |  |  |  |  |  |
| 是否愿意做第二份 |  |  |  |  |  |

## 说明（避免把这张表当能力声明）

- 以上读数是**项目产物读数**（溯源率、缺口类别、证据定位条数、图表分级），不是独立证券分析；
- "验收 pass"只说明确定性检查通过（数字可溯源、引用对得上、声明齐全），**不等于**研究结论正确；
- 费用全部**未知**（无计费口径可读），上表耗时是墙钟时间。
