# 架构监督记录

## 09-16 10:55 当前计划复核与完善

- 用户要求用computer-use完善Zcode长计划；读取当前任务《修0.1版本绑定＋第1节三项缺陷，再一次有界端到端》，只查看当前计划及相关末尾。HEAD仍ab740b5，最近提交不变，修复未提交。对10:29指纹仅展开新增acceptance_checker/test_report_quality及状态；未重复全仓测试或网页浏览。
- 0.2分通道、0.3完整公式/单位/token已改，自报22项通过，未独立复跑整套；0.1版本绑定仍待实现。新离线反例确认：1/2/100白名单可代替缺失财务输入、跨指标/期间计算没有输出未核实状态；无关1000亿元先命中会使正确1000万元计算失败。详情与正负向验收写入《执行路线与验收关卡_20260916》a节，不能将0.2/0.3结项。
- 当前计划保留a版本/b评审/c取消/d经验，补e根预算与观测在联网前、f分开交付/故障/取消测试；补充单一版本权威、PDF manifest而非字节hash相等、根任务评审隔离、prompt refinery准入；M0–M4承接既有长期规划，逐批验收。
- 10:55通过当前计划的空白自由回答框提交修订意见，未选择“批准实施”、未改模型/权限。刷新确认反馈已入当前任务、输入清空；DeepSeek随后确认路线文件读完，将待办换成本轮a–f和关卡并合并计划。最终修订版及a批实现证据仍待验收，不重复发整批。
- 当前文件指纹见docs/evidence/20260916-plan-fingerprints.json，templates仍677B93FD37333744D8BCC0659C007394CA97ABD15CB8DF7826D328434378B2A4。Codex仅改架构/监督文档，未改实现、未启动实机付费任务、未操作终端UI或提交推送；保留门禁及用户改动。

## 09-16 10:29 增量复核与纠偏

- 用户主动要求查看Zcode历程并指挥。仅查当前织光任务近段assistant记录及UI末尾；HEAD仍ab740b5。与09-15指纹比较：orchestrator/web_ui变化，PDF、LiveActivity、ReportViewer、dist、task_context、net_policy及templates未变；未重审未变模块。
- 进展：V1来源/免责声明/版本比较，V2取消/评审退化已有实现；Zcode报告套件900秒超时→离线桩331秒跑完并暴露首稿丢失，再修后完整复跑中止。全套未验收，执行状态有过期长段。
- 只读任务库：新A ui-7e79c26dd1 SUCCESS224s；C ui-0e0d505a92 SUCCESS260s；B ui-4cf6e482e9于01:05:36 SUCCESS_WITH_ISSUES1697s，acceptance空、模型知识代替官方来源，当前RUNNING=0。旧B停止已收尾。不能沿用00:52“仍卡死”结论或凭周期注册日志判断进程崩溃。
- 独立test_cancel_semantics/test_review_degradation共21项通过，EXIT=0，0.380s；额外离线复现银行空/BOGUS裁决修订后放行、非法模式吞错为个人、取消wait返回None后误报Timeout FAILED。静态确认review降级仅存字段未接最终交付，共享字段有并发串扰风险。
- V1独立只读复核3项P1：同一latest acceptance给两稿；JSON与goal拼接破坏结构化派生；错误公式/单位/数字子串获100%计算溯源。可复现样例与代码行已写《架构复核与纠偏_20260916》。B有缺口还增加successful_strategies/template verified，需严格交付门槛。
- 10:28:57经Zcode当前空输入框发出新指令，要求先第0节V1反例，再评审取消/根预算，暂缓连续D/E/F实跑；不重启旧样例、不绕门禁、不改模型权限。10:29刷新确认消息已入当前对话、输入清空，DeepSeek回应先读复核文件第0节，已开始消费。勿重复发送。
- 已查文件与当前指纹：docs/evidence/20260916-review-fingerprints.json。Codex仅写本复核/指令/监督文档和指纹；未修改实现、未提交推送、未启动付费请求。templates仍677B93FD37333744D8BCC0659C007394CA97ABD15CB8DF7826D328434378B2A4。

## 09-15 19:30 真实UI试用（用户主动要求，非静默巡查）

- 当前仓库8080/Redis6379已启动，16/16服务存活；旧8090实例未动。HEAD ab740b5未变，dist/index.html SHA256 45C002D30D96DB2A239705AA91E0E648B3680D8CD49CC10DEF8FFC39862B8A41；templates.json仍677B93FD37333744D8BCC0659C007394CA97ABD15CB8DF7826D328434378B2A4。
- 当前测试ID：A ui-e80c33e6be，B ui-5712f00b24，C ui-6724969aff。C177秒SUCCESS，A用户停止后528秒收尾却显示FAILED；B确认计划后ReAct未收敛，降级摘要后正在生成报告。详见视觉实测报告_20260915.md及证据目录。
- 新证据：用户输入溯源误判；固定公开渠道免责声明；best_report用长度比较保留旧稿；B注入A不相关经验；PDF两页斜线/表头/分页缺陷；时间截断；取消终态错误。只展开orchestrator_v2相应分支、LiveActivity时间、ReportViewer溯源和web_ui导出入口，未重复全量审查/测试。
- 19:29左右经Zcode当前空消息框发送V1–V4指令；19:30观察消息已送达、输入清空、DeepSeek正在读取两份新文档。先V1修订选择与来源语义，后预算/取消、PDF、中文进度。勿重复发送；网络调用点L01未完成仍保留，未验收43/22项自报测试。
- Codex只写报告、规划、监督记录及测试证据，未改业务实现/模型/权限、未提交推送。运行导致evals/cases/auto_grown.json自动增量，未回滚。报告文档包含最小回归矩阵，待DeepSeek实现后定向验收。
- 收尾：B19:30:51报告无官方原文、三项财务指标均未核实，19:32前端请求停止后续反思。任务目标未验收通过。历史页已验证三项可见；文件指纹详见evidence/20260915_live/review-fingerprints.json，下一轮先比较该文件与已查路径，不重读本轮全部日志。已发指令由DeepSeek消费，不再重复干预。
- 19:34 B停止仍未确认终态，反思内部出现504/重试；下一轮优先只查该任务终态及V1新diff，不重复提交实测或下发相同任务。不强杀共享服务；8080保持运行。

- 最近代码巡查：2026-09-15 17:40（Asia/Shanghai）。自动化频率沿用用户配置，本轮未修改；无实质变化静默。
- 最新观察 HEAD：`ab740b5`（本轮未新增提交）；L01 四项纠偏产生新增暂存改动，门禁材料改为《门禁命中证据与复核请求》，仍未提交。不因 HEAD 未变或 dist hash 更新误判停滞。
- 03:07 至 09:10 的巡查：HEAD、工作区状态、执行状态摘要及 templates.json 指纹均未变化；无新增可行动信息。沿用用户收尾后的等待安排，未打开 Zcode、未发送指令、未重复测试；01:05 补修项仍待恢复工作后转交。
- 分工：Codex 决策、审查、验收；Zcode 当前织光任务中的 DeepSeek 实现、定向测试。主任务 `sess_0d05eff0-184c-452a-abfa-67927490b96e`，本次界面显示 deepseek-flash。
- 用户后续在 Zcode 明确要求新环境金融实测及前端改进，已认可该顺序调整，不将 F1 视作擅自扩范围。
- 09-15 最新产品决策：同时面向个体工作者与银行；银行首发经用户确认是投研与对公客户研究。已新增《长期代码规划_个体与银行_20260915》，以共享核心、两种部署配置和 L00–L07 工作包推进，并更新架构指令及自动化目标，保留 180 分钟频率。
- 09:26 规划收尾发现新增未提交前端改动，随即只读主任务末尾记录：用户 09:09 已明确“继续”；DeepSeek 09:24 正在为 Memory/Health 接入演示按钮禁用。此前等待状态已结束，后续可正常监督；未向其重复发送 01:05 整批要求。新前端 diff 由执行者产生，本轮只记录进展、不验收，下一轮按最新差异定向复核。

## 17:40 增量验收与下一步（指令已送达）

- 上轮指令已消费：DeepSeek 17:16 在当前 Zcode 任务完成报告，新增统一 admit_dispatch、Critic 拒绝/清理、心跳 copy_context、协议矩阵；没有停用门禁。测试夹具等价改写已撤销，test_delivery_chain 无当前 diff，test_p0 只保留安全临时目录及清理的独立修复。
- 独立运行 `python -B -X utf8 -m unittest test_task_context`：**30 项通过，退出码 0，0.077 秒**。输出中的 review exploded 是故障注入预期日志；不是实际测试失败。Critic 银行缺身份零评审并回传 ERROR、评审异常与正常返回清理、心跳线程继承及不回流在此范围内接受。未重复前端/重型套件或真实网络测试。
- **L01 协议仍未全通过**：内存调用 admit_dispatch，显式 mode="bnak" 接受缺银行身份载荷（只校验了环境变量，参数路径漏校验）；显式 context:null 被当作无 context 的旧消息；bank 模式下 schema_version=1.9/true 均被转为 1 接受。上述均为实际复现，非仅源码推断。旧消息本地兼容的 gaps 被 admit_dispatch 丢弃，返回空 reason 且接收端无兼容记录，承诺的可观测性未落实。
- 已定收尾契约：模式参数和环境配置统一规范化及枚举校验；仅缺 context 键允许旧消息兼容；v1 版本仅接受非布尔的 JSON 整数；保留本地兼容原因的日志或结构化事件。补模式/类型/版本/缺键矩阵及实际接收边界拒绝后零执行用例，不只补单个样例。
- 后续顺序不变：协议边界收尾 → 网络抓取与登记服务双通道 → L02 统一预算/费用归属 → 金融事实链。银行 tenant/workspace/actor 的可信来源、deadline 同步、实际预算执行、真实隔离与远端 CI 均未因此完成。门禁等待与可独立开展的代码修复分开，不请求或执行绕过。
- **17:39:32 已送达**：获取唯一 Zcode 窗口，工具报告 minimized 后激活、重新绑定；当前织光任务输入框无草稿。发出 17:38 复核及上述收尾/后续要求，只读主任务消息记录确认新文本已存储。未改变模型/权限、未操作终端 UI。下轮检查消费和新增 diff，不重复下发此前四项。
- 本轮已查：task_context、critic_agent、ws_helpers、两个 Worker 的 admit_dispatch 接入、新增行为测试、test_p0 最小 diff、门禁证据材料和执行摘要。Codex 仅修改架构指令与监督记录，未改业务实现、提交或推送。
- 当前 SHA256：task_context.py `A6484FFD05CF477B75FA46F3E0A55AC07DDE7251FDE87D8A7C54410705341A5B`；critic_agent.py `BCB3404A008442AAF166B3269D36BF67ED68964E7E69963E049E348B18CAA46C`；ws_helpers.py `186403FCBA42CFC2F93830C166E261B63410453DD0816DA930BC0979D379C410`；test_task_context.py `F5880CC16F000B9D03B4F46031B5EBF9B1D9F633C54AAB7618D18F93BDC67308`。templates.json 仍为 `677B93FD37333744D8BCC0659C007394CA97ABD15CB8DF7826D328434378B2A4`，保持用户修改。

## 16:44 复核、架构决定与指令送达（历史）

- L00 四项在已测范围内接受。13 时独立运行 Node 行为测试：默认子进程隔离启动报 spawn EPERM，未进入断言；仅加 `--test-isolation=none` 后原有 14 项全部通过，退出码 0（85.5ms）。实际 Memory 处理函数已接入 API 结果判定，MetricsPage 已接入失效清空状态；GET 摘要拦截及 URL 演示退出测试通过。没有独立跑完整浏览器交互或远端 CI，不把纯函数测试称为整体验收。
- L01 13 时 13 项通过；本轮新增 critic/tool_dispatch/结果回显/步骤线程后，16 时独立运行 `python -B -X utf8 -m unittest test_task_context`：18 项通过，退出码 0，0.075 秒。未重跑 DeepSeek 已报告的三个重型套件；源码接线检查的局限单独记录。
- **L01 未通过**：真实 Critic.run 搭配内存消息/评审替身，bank 缺身份仍调用评审 1 次并发布 PASS；完整身份但评审抛错后发布 ERROR，线程上下文仍残留 probe-root。两条复现无模型或真实网络调用。要求拒绝后零评审调用、错误回报和 finally 清理，覆盖后续任务不污染。
- 协议复现：`bnak` 会接受缺银行身份载荷；999 版本被接受，`broken` 版本变为 1。要求模式枚举和解码版本显式拒绝。`call_with_heartbeat` 真实包装器调用 get_task_context 替身：主线程 root 非空、子线程为空，需补上下文传递。同步 Worker 外层会回传解码异常，不重复报不存在的“丢失败回报”问题。
- 网络决定已写入《架构决策_身份与网络边界_20260915》：登记到具体用途/端点的本地模型可用，内容派生 URL 拒绝内网；禁止全局 allow_private，限制重定向、解析与连接不一致、凭据跨源。对现有公网校验器独立 mock：DNS OSError 仍返回 True，100.64.0.1 也返回 True。因此不能把“已调 guard”直接等同安全或误报。真实网络出口防护仍待验证。
- 门禁保持拦截：DeepSeek 16:11 总结提出换到用户终端提交方案，已明确不采用；“拆变量/等价 API 改写后命中下降”不证明风险下降。真正的临时文件修复可保留；误报需受支持的门禁复核，不能通过换工具/改表达绕过。47 条扫描与 49 条提交命中范围不同，其 3/6/38 分类尚未全部独立确认。不修改钩子、基线或权限。
- **Zcode 送达已确认**：首次应用/窗口枚举未返回目标，先保存待发文档。用户指出 Zcode 一直运行后重新获取清单，得到唯一 Zcode 窗口；截图工具明确报告 minimized。激活并重新绑定后恢复操作，不能将最小化或工具未发现写成程序未运行。当前任务停在门禁选择处、输入框无用户草稿；16:44:11 发送简短纠偏，输入框清空并出现运行标记；只读主任务消息记录确认文本存储。没有重启 Zcode、改变模型或权限。
- 已发送内容：读取新决策文档；L00 限定通过；先修 Critic 拒绝/清理、模式/版本、心跳身份四项；按具体端点实现网络策略；保留门禁，独立修复可继续；保护当前暂存及 templates。下轮先查消费和相关 diff，勿重复整批下发。
- 新增已查：task_context、worker_base/async_worker_base、critic_agent、tool_dispatch、orchestrator_v2 派发/步骤线程、ws_helpers、18 项上下文测试、门禁基线申请/处理记录、测试夹具改写差异；前端 L00 helper 与调用接线。F3/F4 大范围视觉内容、真实容器隔离、预算全链及银行认证边界尚未整体验收。
- 指纹 SHA256：task_context.py `46B49DB281CDAAF6D282F4D9D67A14DF7A82BCBCE0815BD6E0590D441B22FB54`；critic_agent.py `7E7B2DA47D0DF2FFD791F76610AB08C4B36CEF219275E7938D04A5496E08A391`；ws_helpers.py `921B57CF5E35B1380476EA7ED33A2C2BB9CC816624239A5467EDFCE5A2DBB2DB`；test_task_context.py `DBB0EFD007B5E6033EA6CE0088AAF64C689C35F41981246932AF42E2A7243B79`。
- 前端指纹：demoGuard.ts `CDCB5B31D0BF5EB17CA31648DFA95F39DBB3C861DE8AE54F6A4C9096580D0A88`；MetricsPage.tsx `C0DCCC62A1212D62D6A1D22E7D3F18F5582D1A1C236084DEB69564528597655B`。templates.json 保持 `677B93FD37333744D8BCC0659C007394CA97ABD15CB8DF7826D328434378B2A4`。
- 本轮 Codex 仅新增/编辑架构与监督文档，没有修改业务实现、提交、推送或部署。下一轮关注上述 L01 行为缺口及网络策略落地，不把门禁等待误判为实现卡死。

## 11:41 巡查与已送达纠偏（历史）

- 执行正常：Zcode 正在实测 F3 报告页、定位并修图表/分享浮层定位问题，不据耗时判定卡住。未提交业务源码仅 ReportViewer.tsx；dist 的删增为重新构建的 hash 产物，不报误删。
- 独立运行 test_deploy_manifest + test_frontend_guards：23 项通过，退出码 0，0.615 秒。独立运行 test_startup_readiness：unittest 报告 7 项 OK，11.532 秒（该复合命令随后读取 Git/指纹，未单独保存 Python 退出码，不将整条命令的 0 当作测试退出码）。未重复全套测试、未安装依赖或启动真实服务。
- ee99dd0 移除 Git 不跟踪的 prompts/ COPY，配置清单用例回退 example；8cbee18/ebfad59 关闭 Redis 内建重试并增加就绪轮询，相关定向验证通过。真实 Docker 镜像构建、真实端到端及远端 CI 仍未独立验收，不能宣称 CI 复绿。
- L00 部分改善：kill-worker 拦截和页面按钮禁用已加入。Node 内存加载当前真实 demoGuard.ts：POST kill-worker 返回 403，底层 fetch 替身未调用；普通 GET memory/summary 和 refresh=1 均仍调用替身；URL ?demo 后 setDemoActive(false) 仍 active=true。仅用替身，无网络或真实付费调用。
- 仍未关闭：Memory 的删除/演化/审批处理函数未检查 res.ok；MetricsPage catch 仍只设错误，旧 m 继续显示。新增 test_frontend_guards 主要检查源码前缀/样式/声明，23 项通过不能证明这些转换正确，要求实际守卫及组件行为测试。
- 门禁处理记录已出现第三次临时移开，并记载“同条命令被拦后拆步执行”。已明确纠偏：记录不构成通用授权，不拆命令绕过门禁；保留命中并走正常复核，允许相关提交保持待处理。本轮没有操作门禁或认证界面，也未把其他扫描器通过当作可停用门禁的依据。
- 推送阻塞据 DeepSeek 最新记录为 GitHub 凭据缺失，代理正常；已要求停止自动重试，等待用户在正常登录界面处理，不索取聊天令牌。只读本地 upstream 计数为领先 5 个提交（含状态文档 de2a825，未主动刷新远端），与状态正文“4 个”的旧计数区分。
- 已送达：11:40–11:41 通过 Zcode 当前任务消息框发送简短 L00/门禁/凭据纠偏，刷新确认队列出现该消息、输入框清空。此前 01:05 因收尾未发送的意见现已转交；待消费，不再重复发整批。保留当前 F3 改动，优先闭合 L00。
- UI 送达过程：窗口布局变化导致首次点击落在非目标窗口，激活并重新观察后恢复；首次输入未显示，重新聚焦后仅重试一次成功。未点击非目标应用，未自动化终端。已读新版 computer-use 技能及指导/接口/确认文档。
- 已查：上述提交清单、部署/配置测试差异、common/launcher/health_registry、demoGuard、Memory/Agents/Health/MetricsPage 调用点、前端守卫测试、门禁与执行状态。Redis 其余构造点未逐项做真实故障实跑；F3 工作区只抽查差异和运行进展，未整体验收。
- 指纹：demoGuard.ts = 929A7419A9A2EA89144099A8FC088FEE75E58C1E49D64F23807450EEE4FBAB57；MetricsPage.tsx = C7831DDB397444A196B8C30D95D90295443FB4F9A95F8CD6AC90C72F8985A048；11:40 ReportViewer.tsx = 6FAC6E7B89517AC76E77DFDB71A42DB2F65F6CE61BF8DC4BC9F0ADDD8AAFAD2F（持续修改中）。templates.json 指纹仍为 677B93FD37333744D8BCC0659C007394CA97ABD15CB8DF7826D328434378B2A4，保护。
- 本轮 Codex 仅改监督/架构文档，未改业务实现、提交、推送或部署。下一轮先确认消息消费，再按新增 diff 复核，不因凭据缺失阻断独立本地修复。

## 01:05 巡查：执行者已按用户要求停止（历史，09:09 已恢复）

- 只读 Zcode 主任务末尾记录：00:24 报告 c600943 补修完成；用户 00:25 要求“确认无误，电脑关机”；00:27 执行者报告模拟服务/临时实例清理完毕。属于正常停止，不是卡住。未打开 Zcode、未发送启动消息；本轮意见仅写入架构指令，待用户恢复后转交。
- 上轮排队指令已消费并形成 c600943，不要重发旧要求。尚未发送的是架构指令新增“2026-09-15 01:05 复核”中的补修项。无新进展时不重复测试或通知，也不因未交付事项自行唤醒 DeepSeek。
- 后端 `_db_task_totals` 已单次 GROUP BY，终态分母和未知状态分类已落实。独立运行 test_metrics_scope **7 项通过，退出码 0，0.062 秒**；仅替换进程内临时目录为工作区继承权限目录、mock Redis 与费用读取，原断言和实现未改。临时目录已清理；未运行全套/CI/真实服务。
- 演示守卫仍未通过：Node 内存加载真实 demoGuard.ts，安装 fetch 包装后，记忆删除和演化 POST 被拦截，但 **POST /api/kill-worker** 与 **GET /api/memory/summary?refresh=1** 均调用了底层 fetch 替身。无真实网络调用。后端确认 kill-worker 会发布终止消息；summary 的普通 GET 冷缓存和 refresh GET 均可能调 LLM，不能以 GET 判定无费用。
- 同一复现确认 `?demo` 后 setDemoActive(false) 仍返回 active=true；UI 与守卫脱节。Memory 删除/演化/审批未检查 res.ok，403 仍报成功。需同步处理入口、处理函数与错误提示，不留到纯视觉工作。
- MetricsPage 刷新 catch 不清除旧 m，先成功后失败仍显示旧指标，错误文案却声称未知；本轮静态确认，尚无组件故障转换测试。
- 执行隔离证据已从“图表 publish 推断 Docker”改为待验证，认可文档纠正。门禁新增命中和恢复记录，但“自行判断误报即可移开”仍写成通用流程，要求纠正；本轮未操作门禁，恢复状态尚未独立核验。
- 已查增量：metrics_collector、test_metrics_scope、test_p0 指标断言、CI 新增步骤、demoGuard、App/store 接入、MetricsPage、AppLayout 和门禁文档；追踪 Memory/Agents 实际调用及 web_ui 对应路由。F2 的 statusMeta/format 做静态抽查，未做整体 UI 验收。
- 基线：c600943 对已提交文件作后续比较；templates.json SHA256 仍为 677B93FD37333744D8BCC0659C007394CA97ABD15CB8DF7826D328434378B2A4。工作区仍仅原 templates 修改及三个监督/历史文档。本轮 Codex 只改监督文档。

## 上轮验收（09-14 22:58，历史记录）

- 沙箱三项纠偏在已测范围内通过：非隔离模式不再报告隔离就绪；移除“挂 socket 即真隔离”的部署承诺；真实 CodeExecutionWorker 的设施故障原样上抛，不进入 LLM 再生成循环。默认及显式 Docker 不回退宿主，非法配置拒绝；缺 Docker 仍可启动不依赖代码执行的工作台功能。
- 独立运行 test_sandbox_isolation：初次 24 项 fixture 创建时遇到 Python 3.14 临时目录 ACL 的 PermissionError，未进入断言。随后仅在本次进程内把该测试的临时目录创建替换为工作区普通继承权限目录，保留实现和全部断言，24 项通过，退出码 0，1.334 秒；未修改测试源码，替代测试目录完成清理。
- 证据范围：mock 故障注入、实际 Worker 控制流与启动路径，不等于真实 Docker 对抗隔离验证。未重复全量套件、未运行远端 CI。DeepSeek 自报的广泛测试与独立验收分开看待。
- F1 登录早退后的 Hook 修复及 ErrorBoundary 静态检查未见本轮新增问题；浏览器转换结果来自 DeepSeek 记录，未独立重跑。F1 整体尚未验收。

## 已发出的 F1 补修与证据纠正（09-14 历史，已消费）

1. 成功率误算：MetricsPage 将全局 total 减 `/tasks` 最近 50 条中的运行数，再以完成减失败推断成功。100 个全运行任务即可被算成 100% 成功。要求后端同范围、同快照聚合，明确终态/分母/统计时间，失败显示未知；覆盖超过 50 个运行任务、全运行、混合终态和接口失败。
2. 演示边界不实：全局承诺不调用真实接口，但 Settings、Memory、MetricsPage 仍访问真实服务，管理操作可真实保存、删除或触发演化。要求收窄文案、明确真实只读数据，禁用产生真实变更/费用的入口及处理函数，mock 验证无请求发出。
3. 容器证据不足：状态文档将图表 grade=publish 当作 Docker 间接证明，但 charts_pipeline 内置渲染使用宿主 subprocess，grade 表示图表质检。要求直接执行证据或改为待验证；这不等于断言实际容器一定没有运行。
4. 门禁处理记录：本次 Mimosa 扫描拦截后临时移开钩子；22:55 界面已自报恢复，尚未独立核实。要求补本次命中文件/规则、误报判断依据及恢复证据，不把 09-04 的特定用户授权扩为通用例外。Codex 未修改门禁或系统设置。

- 上述要求已写入总架构师执行指令.md 的“F1 补修要求”，22:57 经 Zcode 当前任务输入区发送；刷新看到已进入消息队列、输入区清空。待消费，不要重复发送。
- 下一次先检查消费情况与相关补修 diff；正常执行不打断。补修后继续用户已授权前端工作，再跟踪根任务预算、金融事实语义；不直接替 DeepSeek 编码。
- 状态文件仍有新旧 Docker 验证口径冲突、前端演示范围过宽及下一步滞后，已要求同步修正。CI 结论须绑定提交，不因本地成功或网络阻塞推断远端状态。

## 变更基线与保护项

- 本轮 Codex 仅修改架构指令及监督记录；未改业务实现、未提交、推送或部署。
- 22:58 工作区：templates.json 为接手前已有修改；三个架构/历史文档未跟踪。保护用户修改。
- templates.json SHA256：677B93FD37333744D8BCC0659C007394CA97ABD15CB8DF7826D328434378B2A4（本轮未变）。
- code_sandbox.py：C83F65751BB0FF58C7B317622873A0F862D96924B3FD648E1641EFAEFD485975。
- test_sandbox_isolation.py：41C4AA4766F888B8D609414325C2C69547A01A4B28DF78BC47E4F0FD6038EC6A。
- workers/code_execution_worker.py：CEA4F83C213A5085DE6B14DE7D0238F6FD357D183D6B35C29B27BB382FEC0CA6。
- F1 其他文件以提交 f579eec 作比较基线；不因打包资源 hash 变更重复报警。

## 已闭合的监督历史

- 17:13–17:16：DeepSeek 收到并承认默认 auto/FALLBACK 与新隔离要求冲突，开始纠正。
- 17:39：收到“无 Docker 可启动，缺隔离只停用代码执行”边界；承认输出管道不能代替真实退出码。
- 18:07：指出隔离状态误报、compose socket 承诺未验证、真实 Worker 吞设施错误三项；本轮已按上文范围闭合。
- 项目历史与执行特点见项目历史与执行协作画像_20260914.md，其“当前状态”为早期接手快照；最新状态以本文件为准。
