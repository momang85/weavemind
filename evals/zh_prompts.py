# -*- coding: utf-8 -*-
"""Ragas 式中文评测提示词（对标标准 2.4 与 course_core/ragas_prompt）。
自包含实现，不依赖外部框架；每条指标输出严格 JSON。"""

ANSWER_CORRECTNESS = """你是答案正确性评审员。【角色】严格的事实核对员。【受众】评测系统（机器可读）。
【任务】对比 参考答案(ground_truth) 与 模型回答(answer)，判断回答的事实正确性：
完全一致或包含所有关键事实 → 1.0；部分正确 → 0.5；包含事实错误 → 0.0。
【输出要求】只输出 JSON：{{"score": 0.0|0.5|1.0, "reason": "一句话理由"}}

参考答案：{ground_truth}
模型回答：{answer}"""

CLAIM_EXTRACTION = """你是论断提取员。【角色】严谨的文本分析师。【受众】评测系统（机器可读）。
【任务】从模型回答中提取所有可独立核验的事实论断（claim），每条约 10-30 字。
【输出要求】只输出 JSON：{{"claims": ["论断1", "论断2"]}}

模型回答：{answer}"""

FAITHFULNESS_NLI = """你是事实一致性评审员。【角色】严格的事实核验员。【受众】评测系统（机器可读）。
【任务】逐条判断论断是否被 参考资料(context) 支持：能从参考资料中直接推断 → true；参考资料未提及或与参考资料矛盾 → false。
【输出要求】只输出 JSON：{{"supported": [true, false, ...]}}，顺序与论断一一对应。

参考资料：{context}
论断：{claims}"""

CONTEXT_RECALL = """你是召回率评审员。【角色】严格的检索质量核验员。【受众】评测系统（机器可读）。
【任务】参考答案中的每一条关键事实，是否能从 参考资料(context) 中找到依据：
能找到 → true；找不到（资料缺失/无关）→ false。
【输出要求】只输出 JSON：{{"attributable": [true, false, ...]}}，顺序与参考答案要点一一对应。

参考资料：{context}
参考答案：{ground_truth}"""

CONTEXT_PRECISION = """你是检索精度评审员。【角色】严格的检索质量核验员。【受众】评测系统（机器可读）。
【任务】对每一条参考资料，判断它是否与问题相关且有助于回答：相关 → true；无关/干扰 → false。
【输出要求】只输出 JSON：{{"relevant": [true, false, ...]}}，顺序与参考资料一一对应。

问题：{question}
参考资料（按相关度从高到低排序）：{contexts}"""
